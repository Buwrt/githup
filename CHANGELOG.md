# 更新日志

从 **v1.1.3** 开始记录。每发一个版本，在文件最上面加一节，格式照抄下面这份就行。

版本号规则与更新策略见 [README → 更新机制](README.md#更新机制)：**前两位变 = 强制更新，只有第三位变 = 可选更新。**

---

### 修：朴素样式下底栏的文字不居中（魅族 20 Pro 等）

**现象**：关掉 iOS 风格后，底栏「首页 / 通知 / 探索 / 搜索 / 我的」的图标和
文字整体偏上，选中项那枚浅灰底衬压着图标 —— 1+ Ace 上看着正常，魅族 20 Pro 上一眼就不对。

查下来是**两个独立的 bug 叠在一起**：

| # | 根因 | 修法 |
|---|---|---|
| 1 | 指示器 `.tab-ind` 的四条边写死 4px，是给玻璃胶囊（有 4px 内衬）配的；朴素样式的底栏没有内衬，那个 4px 就没有参照物，灰斑自然对不上字形 | 抽出 `--nav-ind-top/bottom/left/height/width`，两种风格各写一套 |
| 2 | **真正让文字偏上的那条**：`#tabbar` 是 `align-items: stretch`，`.tab` 的高度被拉满整条底栏 —— 底栏自己的上下 `padding` 对 `.tab` 的版心**完全无效**，内容永远按整条 64px 居中 | 内衬改写到 `.tab` 上（`padding: var(--tabbar-pad-y) 0`） |

第 2 条解释了「为什么魅族 20 Pro 明显、1+ Ace 看不出来」—— 它不是「某些机型不对」，
而是**一直在错**，只是玻璃模式下 `--nav-pad` 只有 4px、`#tabbar` 的总高也带 padding、
两次错误互相抵消了一部分，差得少看不出来。朴素样式把 `--nav-pad` 归零，
第 1 条又把灰斑钉在顶部，两个错误一起露出来。

顺带把朴素样式的选中项换成 **安卓绿 `#3ddc84`**：classic 是「Android 原生那一套」，
主题轴不该连 App 的形态风格一起改，所以用 `html[data-nav="classic"]` 明确钉住
（特异性 (0,1,0) 与 `html[data-theme="dark"]` 相同，本规则在文件更靠后，深色下也拿得到绿色）。

实测（字形组中心 vs 底栏几何中心，五档机型 × 两种风格）：

```
朴素 393×852 / 360×780 / 320×569 / 480×1040 / 852×393 横屏
     字形中心 31.5 / 31.5 / 31.5 / 31.5 / 25.5   条中心 32 / 32 / 32 / 32 / 26   偏差 0.5px
     指示器中心偏差 0.0px（五档全部）
iOS 同上五档：字形偏差 0.5px，指示器偏差 0.0px，回归无变化
```

### 修：朴素样式下页面内容被顶栏压住

`#view` 那段「让开顶栏」的 `padding-top` 原来只写在 `html[data-nav="glass"]`
选择器里 —— 换句话说**关掉 iOS 风格，这段留白整个消失**，第一行内容直接顶到顶栏底下。
顶栏在两种风格下都是 fixed，留白本来就不该分风格，现在提到 `#view` 的基础规则里。

### 改：仓库页页签对齐 GitHub 官网

| 项 | 改前 | 改后 |
|---|---|---|
| 图标 | 每个页签都挂一个 SVG | **全部去掉**（官网只有文字，`< > 代码` 那对尖括号本身就是字符画的） |
| 高度 | 「上下 padding + 内容高」撑出来的，带图标 49px / 不带 39px | 固定值：普通 48px、iOS 风格 49px |
| 文字居中 | 偏上 1.5px | 0（`line-height: 1` 让行盒贴住字形） |

文字偏上 1.5px 是另一个坑：中文字体（Noto Sans CJK / PingFang）的正常行高是 1.4~1.5，
一半分给上半、一半留给「下伸部」—— CJK 根本没有下伸部，那半行空白就白挂了。
再加上 `box-sizing: border-box` 会被两个边框吃掉 3px（外层 `1px` 分隔线 + 按钮 `2px` 选中线），
内容可用高度只剩 45px，文字按 45 居中、视觉上是 48 的条。两个坑一起修：
`line-height: 1`，选中线改成不占盒模型的 `inset` 阴影，外层用 `content-box`。

以上三处包：**817,108 字节**，SHA-256
`58785a5836a4ee49c9fdd850c72b079561034a1a1ba8d17bdb36351d9f863230`，源码指纹
`423dcfc9edf0c08545bd58dbfce101210a353c102e999e96d10a84fc855ca4c0`。

---

## 1.1.5（新手指导 / 顶栏标题靠左 / 搜索按钮放大 / 全机型适配）— 2026-09-20

> 同一个对外版本号 1.1.5（versionCode `1001005`）下的第四个包，可**直接覆盖安装**。
> 新增内容：详细新手指导（十步高亮，可跳过，设置里可重看）、顶栏标题改回左对齐、
> 搜索按钮与顶栏按钮放大、全机型适配（左右安全区 / 窄屏 / 大屏 / 横屏 / 字体缩放 / 老 WebView 玻璃降级）。
> 之前的三个包依次是：悬浮玻璃底栏 + 5 条加速通道 + 坏包与漏提醒修复、底栏风格开关、本次的新手指导。

包：812,656 字节，SHA-256 `abf8cab39dc912f0a5cf37f1deb56511a6c26579cc8a39cdc50906e7dcddc276`。
源码指纹 `SRC_SHA256`：`30ef69af8cc98d5b5ed51bdaff359845b7650ef2a0c50c0ed261b31e304dc902`。

### 新：详细新手指导

首次打开自动播放十步高亮引导（欢迎 → 顶栏 → 右上角 → 底栏 → 搜索框 → 搜索类型 →
探索时间范围 → 探索语言 → 设置里的玻璃开关 → 结束），随时可跳过，返回键也能退出；
状态记在本地只播一次，**设置 → 新手指导** 可重看，并显示「已完成 / 未开始」。
洞按目标元素实时定位，目标未渲染就轮询等待，拿不到就退化成居中卡片；
气泡会根据空间自动换边并避开「跳过」按钮；结束或跳过都回到引导前的页面。

实现：`css/onboarding.css` + `js/onboarding.js`（`Onboarding.start/quit/isActive/isDone/autoStart/restart/reset`），
`app.js` 的返回键把引导放在最高优先级（否则用户会被困在遮罩里），`boot()` 在首屏渲染后再启动引导。

### 新：设置里可以重置新手引导

新增一行 **重置新手引导**（`Onboarding.reset()`）：只抹掉「已看过」的记录、**不**立刻播放，
效果是下次打开 App 时自动重播一遍。和它上面那行 **新手指导** 分工不同 ——
那行是「现在立刻再看一遍」（`restart()`），这行是「下次打开再看」。
点完设置页的两行当场刷新（「可重置 → 已是初始状态」、「已完成 → 未开始」），
本来就是没看过的状态时提示「已经是初始状态」而不是空点一下。

### 改：设置里的「液态玻璃底栏」改名「iOS风格」

只改显示名：开关的行为、默认值（开）、`data-s="glass"` 都不动；
引导第 9 步的文案和高亮目标跟着同步，设置页的说明文字、`api.js` / `app.css` 里的注释也一并改掉。

### 改：顶栏标题靠左、按钮放大、全机型适配

- 五个页签的顶栏标题统一 `text-align: left`（上一版居中，中文标题居中读起来是散的）。
- 搜索输入框与按钮最小高度 40px（玻璃主题 44px），顶栏图标按钮 36 → 40px，按钮加 `flex: none` 防被压扁。
- 新增 `--safe-l` / `--safe-r` 左右安全区（原生 `JsBridge.safeInsets()` 扩展为四值）；`100dvh` 优先；
  窄屏 ≤360px、大屏 400~719px、横屏（高 ≤520px）各一档断点（媒体查询里必须写 `:root` 才压得住文件开头的变量）；
  锁 `text-size-adjust: 100%`；`html[data-glass-support="0"]` 自动降级为近实色。

---

## 1.1.5（悬浮液态玻璃胶囊底栏 / 下载通道扩充 / 底栏风格开关）— 2026-09-20

文件名 `githup-1.1.5.apk`，对外版本号 **1.1.5**（versionCode `1001005`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 改：底栏换成悬浮的液态玻璃胶囊

按用户截图逐像素量出来重做（细节见下方「三段结构」一节）。同时修掉一个真 bug：
设置里选「跟随系统」、系统是浅色，底栏却显示成深色 —— 根因在原生层，
WebView 的算法深色化会绕过 CSS 自动反色整页，`prefers-color-scheme` 在 WebView 上也不可靠。

### 新：设置里可以关掉液态玻璃

`设置 → iOS风格`（原「液态玻璃底栏」），**默认打开**。关掉后底栏回到贴底的朴素样式。
两套取值通过 `html[data-nav]` 切换，与深浅主题（`html[data-theme]`）是两条独立的轴 ——
所以「跟随系统 + 玻璃」这种组合天然可用。

### 改：下载加速通道从 3 条扩到 5 条，并按实测速度重排

原来的顺序是按「谁最知名」排的，实测下来恰好不是最快的那几个。逐条实测后重排，
并逐个验证「拿到的确实是文件本身」而不是目录页或错误页。详见 `DownloadChannels` 的注释表。

### 修：下了个坏包却提示「下载完成」

镜像返回残缺文件（下到一半断了却报 200）或被重定向到 HTML 页面时，
DownloadManager 都会当成功。现在完成时比对大小，对不上就换道重下。

---

## 1.1.4（App 内直接建拉取请求 / 更新提示不再反复弹 / 返回键回到该回的地方）— 2026-09-20

> **2026-09-20 回档**：这一节原先还包含「毛玻璃质感」，该特性已整体撤销，
> 代码与文档都已回退到毛玻璃引入之前的状态。版本号**保持不变** ——
> 对外 `1.1.4`、内部 `1001004`，本次只是同一版本号下的又一个包。
>
> 撤销的原因：毛玻璃在两个方向上都没做成 ——
> 一是它的代价不好接受（`backdrop-filter` 要动导航栏的定位方式，
> 副作用是首屏被吃掉、提示条撞成白底白字这类连带问题）；
> 二是它的收益在这套配色下兑现不了 —— GitHub 官方主题本身就是大面积浅灰白，
> 白色半透明叠在白色画布上算出来只差 3 个色阶，肉眼分辨不出。
> 花了两个来回确认这条路在本项目里走不通，所以撤掉，把代码还原干净。

文件名 `githup-1.1.4.apk`，对外版本号 **1.1.4**（versionCode `1001004`）。
本次回档后的包：774,725 字节，SHA-256 `346f6b88a06e2f84d101690b118e53bde92be7f0cba073d6852206ba0089f4c0`。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 新增：App 内直接创建拉取请求（不再把人推去浏览器）

**现象**：在仓库的「拉取请求」页点右下角按钮，弹出来的是一句
「创建 PR 需要选择源分支与目标分支，建议在网页端完成。是否前往浏览器？」——
等于没有这个功能，还得跳出去在网页上重来一遍。

**原因**：这个入口一直只是个「占位」—— 点开浏览器就结束了，App 里从来没做过
创建 PR 的表单。`newIssue` 早就做好了（标题 + 正文 + 标签 + 插图），
PR 这边缺的是同一套东西加两个分支下拉。

**现在**：右下角按钮直接打开完整的创建表单，跟网页版对齐：

- **来源仓库** —— 默认猜成你自己的同名 fork（跨仓库提 PR 的常规姿势），
  也支持 `owner/repo`、`owner/repo:分支`、`owner:分支` 四种写法；
  只写一个分支名就当作本仓库的分支
- **源分支 / 目标分支** —— 从 `/branches` 拉真实分支列表填进下拉；
  两边是同一个仓库时只请求一次，不浪费配额
- **实时比对** —— 选完分支立刻显示「N 个提交 · M 个文件变更」，并挑最新提交的
  标题兜底填到标题栏；两个分支没差异会当场提示，不用等 422 回来才知道
- **标题 / 说明** —— 说明框同样支持 Markdown 工具栏和插图（复用议题那套）
- **草稿 / 允许维护者修改** 两个开关
- 建完直接跳到新 PR 的详情页

### 几个容易踩坑的地方

| 坑 | 处理 |
|---|---|
| `head` 的写法分两种 | 同仓库只写分支名；跨仓库才写 `owner:branch`。写成 `owner:branch` 指向自己仓库时，GitHub 会当成「从 owner 的同名 fork 拉」，直接报 head 无效 |
| 422 报错太术语 | `Validation Failed` 翻译成人话：已有未关闭的 PR / 没有差异 / 分支不可读 / 没权限 |
| 用户改了来源仓库却没触发 change | 提交前**再解析一次**，避免「以为提到了 A，实际提到了 B」 |
| 找分支要额外一次请求 | 新增 `API.cachedGet()` 只读内存缓存：来源仓库刚才在列表或详情页看过就直接命中 |

`/compare` 特意不吃缓存 —— 刚推的提交必须立刻看得见。

### 修：1.1.4 的 Release 正文里还挂着 1.1.3 的更新日志

**现象**：打开 v1.1.4 的 Release 页面，往下翻全是 1.1.3、1.1.2 的改动记录 ——
一屏半都是上一个版本的日志，看着像发错了包。App 内的「Release 详情」同样如此。

**根因**是发版流程把「累积文件」当成了「单版本正文」：

```
.github/workflows/publish-release.yml
    body_path: RELEASE_NOTES.md      ← 整个文件
```

`RELEASE_NOTES.md` 按设计是**累积**的 —— 每个版本一节，新的加在最上面，
历史留着备查，这是刻意的（翻旧版本时能查到当时那个包的原始大小和 SHA-256）。
但 Release 页面的正文必须是「这一个版本」的内容，把整个文件贴上去，
历史就跟着一起进了 v1.1.4 的页面。

**改法**是发布前先抽出当前版本那一节，文件本身保持完整：

- 新增 `tools/extract-release-notes.py`，按 `## githup vX.Y.Z` 切小节，
  取指定版本那一段；标题里没写版本号的行不算小节开头，免得把正文里的
  `##` 误判成新一节
- workflow 里加一步「抽出当前版本的发布说明」，`body_path` 由
  `RELEASE_NOTES.md` 改成抽出来的 `/tmp/release-body.md`
- 手动触发（`workflow_dispatch`）时 `GITHUB_REF_NAME` 是分支名、取不到
  对应小节，退回「文件里最上面那一节」—— 同样是最新版，不会发空正文
- 取不到对应版本时脚本**报错退出**（退出码非 0），workflow 直接失败，
  不会静默发一个空说明出去

已经发布的 v1.1.4 Release 正文已按同样的方式改好，现在只剩 1.1.4 一节。

### 修：下载变慢 / 卡在 0 B 不动

**现象**：点「立即更新」后进度条长时间停在 0 B，或者只有几十 KB/s 慢慢爬。

**根因**是一个「记住上次通道」的优化把直连推到了最前面：

```java
// 修改前
if (DIRECT.equals(lastChannel)) out.add(0, url);   // 直连提到首位
```

`last_channel` 这个偏好是**全局**的，不分文件也不分时间。只要**某一次**下载
恰好直连走通了（哪怕是个小文件），直连就被永久记成「优先通道」，之后
**每一次**下载都先试直连。而国内网络下直连 GitHub 的 Release 附件
（会 302 到 `objects.githubusercontent.com`）正是最慢的那条。

更糟的是换道有代价，得熬过一段「干等」：

| 参数 | 原来 | 现在 |
|---|---|---|
| 宽限期（首次出数据前不判慢） | 8 秒 | **3 秒** |
| 监控间隔 | 3 秒 | **1.5 秒** |
| 连续几轮判慢才换道 | 2 轮 | **1 轮** |

原来最坏要 `8 + 2×3 = 14 秒` 才切走 —— 用户眼睁睁看着 0 B 十几秒。
现在最坏约 `3 + 1.5 ≈ 4.5 秒`。

**改法**：

1. **直连永远排最后兜底**，不再参与「上次走通就优先」的排序。
   海外用户绕代理确实慢一点，但「慢一点」远好过「先卡十几秒」。
2. **不再记录直连成功** —— 只记哪条**加速镜像**通了，语义才干净。
3. **换道阈值收紧**，见上表。

带令牌的地址仍然**一律直连**，绝不走第三方代理 —— 那是硬约束，不是性能取舍。

**验证**（`DownloadChannels` 是纯逻辑类，测试直接从 Java 源码解析，不另抄副本 ——
抄副本的话改了 Java 忘了改副本，测试就成了「绿灯橡皮章」）：

| 场景 | 期望 | 结果 |
|---|---|---|
| 首次下载 | 加速镜像在最前 | ✅ |
| **直连曾被记过** | **仍然加速在前** | ✅ |
| 某镜像上次走通 | 该镜像优先 | ✅ |
| 非 GitHub 域名 | 不走代理 | ✅ |
| 带令牌 | 绝不走代理 | ✅ |

### 修：Release 页面显示的是 1.1.3 的说明

**现象**：v1.1.4 这个 Release 的说明栏里，写的是 1.1.3 的内容。

**根因**不在代码里，在发布流水线：Release 的正文来自
`.github/workflows/publish-release.yml` 里的 `body_path: RELEASE_NOTES.md`，
Actions 把整个文件原样贴上去。而发 1.1.4 的时候没人往
`RELEASE_NOTES.md` 顶部加 1.1.4 这一节 —— 文件最上面还停在 1.1.3，
于是被贴成了 1.1.3 的内容。

**改法**：补上 1.1.4 这一节（含真实大小与 SHA-256）。

### 修：重新下载同一个 1.1.4 后，还会弹「有内容更新」

**现象**：从 Release 下载装上 1.1.4，打开后又被弹一次更新提示。

**这是两个不同的事叠在一起**：

1. **同一版本号下反复换包**。1.1.4 这个版本号先后出过几个内容不同的包，
   每替换一次 Release 附件，已装用户的本机指纹就跟服务端对不上，
   一查就是「内容有更新」。检测逻辑本身没错 —— 包确实换了。
2. **但提示不说人话**。原来写「版本号仍是 1.1.4，但内容已经变了」，
   用户看到的是「我装的就是 1.1.4，怎么又 1.1.4？」

**改法**分两头：

- **让每个包有唯一身份**：`versionCode` 从 `1001004` 提到 `1001005`，
  `versionName` 仍显示 1.1.4（Android 允许两者不联动）。
  这样系统层面认得出「这是新包」，能正常覆盖安装。
- **把话说清楚**：标题从「有新内容可用」改成「这个安装包已更新」，
  正文改成「你装的这个包和服务器上的不是同一份」，并列出两份包的指纹前 8 位，
  让用户和排查问题的人一眼确认确实是两份不同的包。

顺带修了 `tools/gen-guard.py`：官方下载地址原来手写死成 v1.1.3，
发版漏改就会把被拦下的用户送到上一个版本 —— 现在从 `build.gradle` 读版本号自动拼。

### 修：点了「立即更新」，更新提示还一直弹

**现象**：弹出「有新内容可用」，点「立即更新」，下载也开始了 ——
但切回前台或重开一次 App，同一个提示又弹出来，反反复复。

**根因**是「本机指纹」和「期望指纹」之间存在一段必然对不上的空窗期。

更新检测靠两个指纹比对：

| 名字 | 含义 | 什么时候变 |
|---|---|---|
| 期望指纹 | 服务端那一份包的 SHA-256 | 服务端一换包立刻变 |
| 本机指纹 | 已安装 APK 的 SHA-256（`Native.apkSha256()`） | **要等新包真装上才变** |

点「立即更新」之后，中间隔着「下载 → 拉起系统安装器 → 用户在安装器里点确认」，
这段窗口里本机指纹**仍然指向旧包**。可 `autoCheck()` 每次切回前台都会真的重查，
一查必然是「不一致」，于是判定「有内容更新」，再弹一次 —— 循环往复。

历史记录里「跳过此版」有个 `SKIP_SHA_KEY` 能压住同一个包，
但「立即更新」这条路径从来没记过这笔账。

**改法**是让「立即更新」也真的算数：点下去、且**确实开始下载了**，
就把那一份的指纹记成「正在装」。

```js
var started = install(info);
if (started) markUpdating(info);   // 只有真的开始下载才记账
```

几个关键取舍：

- **只记指纹，不记版本号**。指纹 = 「是哪一个包」，同一个包不会装两遍，
  忽略它没有代价。版本号 = 「哪一代」，同一代可能被重新打包成不同的包 ——
  那是个全新的包，指纹对不上，照常提醒。
- **`install()` 返回 false 就不记**。没有 APK 附件 / 内置表与清单冲突 /
  拿不到指纹这三种情况都会退化成「打开官方下载页」，那不算「正在装」——
  记了反而会把后续提醒一起压住，用户再也收不到更新。
- **强制更新不吃这套**。第一、二位版本号变化时不认「跳过」也不认
  「正在装」，照旧不可关闭地拦。
- **装上之后自动失效**。记的指纹只在「本机指纹仍是当初那个」期间有效；
  新包装上、本机指纹追平，这条记录自然退休，不残留。
- 卸载重装、清应用数据，记录一并消失，新用户第一次打开照常提醒。

**验证**（真实 `updater.js` 在沙箱里跑，18 项断言全过）：

| 场景 | 期望 | 结果 |
|---|---|---|
| **点「立即更新」后再检查** | **不再弹** | ✅ |
| 反复打开 App 十次 | 只在第一次弹 | ✅ |
| 新包装上后 | 不再提醒 | ✅ |
| 同版本号又换了个新包 | 照常提醒 | ✅ |
| 「跳过此版」 | 仍然有效 | ✅ |
| 第二位数变化（强制） | 照常拦，跳过/正在装都不认 | ✅ |
| 拿不到指纹只能打开下载页 | 不误记「正在装」 | ✅ |
| 清了应用数据 | 正常提醒 | ✅ |

对照实验：同一套测试跑改动前的 `updater.js`，「点立即更新后不再弹」这项
**失败**（弹窗从 1 次涨到 2 次），跑改动后的通过 —— 确认症状可复现且已消除。

### 改：在任意标签页按返回，回到首页而不是退出应用

**以前**：在「通知」「探索」「搜索」「我的」上按返回键，因为没有上一层可退，
直接落到「再按一次退出」—— 稍微多按一下 App 就没了。

**现在**：这些标签页上按返回，先切回首页；只有在**首页**再按返回，才进入
「再按一次退出」。

返回键的完整优先级：

| 顺序 | 拦到谁 | 动作 |
|---|---|---|
| 1 | 图片查看器 | 关掉查看器 |
| 2 | 弹层 / 确认框 / 菜单 | 关掉弹层（强制更新弹层除外，它要吞掉返回键） |
| 3 | 纵向层级（详情 / 议题 / Release…） | 逐级退回上一层 |
| 4 | 停在某个标签页上 | **回到首页** |
| 5 | 已经在首页 | 交给「再按一次退出」 |

改动在 `App.handleBack()` 的第 4 步：

```js
var here = (location.hash || '#/').replace(/^#/, '').split('?')[0];
if (here !== '' && here !== '/') {
  Router.go('/');
  return true;
}
return false;    // 就在首页，交给「再按一次退出」
```

这里用 `Router.go('/')` 而不是 `history.back()`：横向切标签走的是 `replaceState`，
history 里根本没有标签之间的条目可退（上一版刚这么改的）。

**纵向层级完全不受影响**：探索 → 仓库详情 → 议题，按返回仍然是
议题 → 详情 → 探索，再按一次才回首页。

### 修：从探索页进「下载管理」，按返回被甩回首页

**现象**：在「探索」页点右上角的下载图标进下载管理，按返回键没有回到探索页，
而是直接跳到了首页。

**根因**是 `TAB_ROOTS`（标签根页面名单）里混进了不该在里面的东西：

```js
// 修改前 —— /downloads 和 /login 被当成「底部标签根页」
var TAB_ROOTS = ['/', '/notifications', '/explore', '/search', '/downloads', '/profile', '/login'];
```

下载管理是从**右上角图标**进去的，登录页是流程里的，底部导航栏上都没有它们的
位置，本质上是内页。但一旦被列进 `TAB_ROOTS`：

1. `pushRoute()` 判定「当前在标签页 + 目标也是标签根页」= 横向切换，走
   `replaceState` —— **不堆历史、深度不加**；
2. 于是按返回时 `canGoBack()` 为 false，直接落到上面第 4 步「回首页」。

**改法**是让名单名副其实 —— 判据是「底部导航栏上有没有它的位置」，
不是「路径看起来短不短」：

```js
var TAB_ROOTS = ['/', '/notifications', '/explore', '/search', '/profile'];
```

顺带把 `isTabPath()` 里抄的第二份名单也改成复用 `TAB_ROOTS`。两处各写一遍，
改一处漏一处，正是这类问题的温床。

现在从任何页面进下载管理都是 `pushState`（真的深入一层），按返回退回
**进来前的那个页面**；在真正的底部标签上按返回才回首页。

**验证**（用真实 `app.js` 在沙箱里跑，`handleBack` / `pushRoute` / `Router` 全是从源码抽取执行）：

| 场景 | 期望 | 结果 |
|---|---|---|
| 通知 / 探索 / 搜索 / 我的 页按返回 | 回首页且不退出 | ✅ |
| **探索 → 下载管理 → 返回** | **回探索页**（不是首页） | ✅ |
| **我的 → 登录 → 返回** | **回「我的」** | ✅ |
| 仓库详情 → 下载管理 → 返回 | 回仓库详情页 | ✅ |
| 首页 → 下载管理 → 返回 | 回首页 | ✅ |
| 首页按返回 | 不消费，交给「再按一次退出」 | ✅ |
| 探索 → 详情 → 议题 → 返回 → 返回 → 返回 | 议题→详情→探索→首页 | ✅ |
| 首页 → 通知 → 探索 → 返回 | 直接回首页（不倒着走标签） | ✅ |

---
## 1.1.3（修：返回键在标签页之间倒着走）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 修：首页→通知→探索，按返回会逐个标签倒着退回去

**现象**：在「首页」点「通知」，再点「探索」，然后按返回键 ——
退回「通知」，再按退回「首页」，再按才退出应用。
底部标签是并列的一级入口，用户预期是「在探索按返回就该退出」。

**根因**是两处互相矛盾的设计：

1. `pushRoute()` 里横向切标签**故意不加深度**：

   ```js
   var isLateral = Router.isRoot(path) && Router.isRoot(location.hash || '#/');
   if (!isLateral) routeDepth++;      // 横向切标签，深度不变
   ```

2. 但 `canGoBack()` 又有一条兜底，反过来认定「横向也有一层可退」：

   ```js
   if (routeDepth > 0) return true;
   var now = (location.hash || '#/').replace(/^#/, '').split('?')[0];
   return now !== '' && now !== '/' && this.isRoot(now);   // ← 停在标签页就返回 true
   ```

于是深度虽然不涨，`canGoBack()` 却始终为真，返回键就沿着 `history` 里那些
标签条目一条条往回走。

**改法**：

- `canGoBack()` 只看纵向深度 —— `return routeDepth > 0;`
  标签根页上按返回即「没有上一层」，交给「再按一次退出」。
- `pushRoute()` 里横向切标签改用 `replaceState`，不再往历史里堆条目。
  留痕就会变成「返回键把标签倒着走一遍」，不留痕才是并列入口该有的样子。
- `TAB_ROOTS` 补上首页 `'/'`：原来漏了它，导致「首页↔其他标签」被误判成纵向深入，
  深度会莫名其妙地涨。
- 横向判据收紧成「**当前位置**也是标签页」：

  ```js
  var hereIsRoot = Router.isRoot(location.hash || '#/');
  var isLateral = hereIsRoot && Router.isRoot(path);
  ```

  这样「从仓库详情点底部标签栏」仍旧走 push —— 那是真的往回退了一层。
  若图省事只看目标位置，`replaceState` 会把详情页那条历史覆盖掉，
  用户再按返回就直接退出应用（`Router.go` 里早有一条注释警告过这个坑）。

**验证**（用真实 `app.js` 代码在沙箱里跑，不是重写的副本）：

| 场景 | 期望 | 结果 |
|---|---|---|
| 首页 → 通知 → 探索，按返回 | 直接栈底（退出） | ✅ 历史仅 1 条，`canGoBack=false` |
| 首页→我的→搜索→通知→探索 乱序切 | 同上 | ✅ |
| 探索 → 仓库详情 → 议题，逐级返回 | 议题→详情→探索 | ✅ |
| 首页 → 详情 → 返回 | 回到首页，且不再可退 | ✅ |
| 详情页 → 点标签栏 → 返回 | 回到详情页 | ✅ |
| 横切多标签 → 进详情 → 返回 | 回到进详情前停留的那个标签 | ✅ |

纵向层级（详情页、议题、Release、代码…）的回退行为完全不变。

---
## 1.1.3（修：主动搜索后搜索栏少三样东西）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 修：在搜索页里打字搜索，清除按钮 / 搜索按钮 / 排序筛选行不出现

**现象**：从底部「搜索」页签进来，在输入框里输入关键词、按回车 —— 结果出来了，
但输入框右边没有清除按钮、没有「搜索」按钮，类型切换下面的「最佳匹配 / 筛选」那一行也不见。
换个页签再切回来，三样东西又都冒出来了。

**根因**：这三样的显隐在渲染时就定死了，判据是地址栏里的 `q`：

```js
(q ? '<button class="clear" id="clr">…' : '') +      // URL 里没 q 就不生成节点
(q ? '<button class="btn" id="go">搜索</button>' : '') +
(q ? '<div class="chips" id="sflt">…' : '') +
```

而「在搜索页里打字然后回车」走的是 `doSearch()` —— 它只把结果区 `#sres` 重画一遍，
**页面整体不重新渲染，地址栏一动不动**。于是那三样永远等不到 `q` 变非空。
切页签会触发一次完整渲染（`Router.go`），所以看起来像是「切一下才有」。

**改法**：把「渲染时机」和「显隐判断」拆开。

- 三样一律先渲染出来，初始 `hidden` 只作首屏参考；
- 新增 `syncToolbar()`，每次输入都按**输入框里的实际内容**切显隐；
- 挂了两个 input 监听：一个防抖的负责发请求（保持原逻辑），一个不防抖的专门管按钮 ——
  打字、退格、粘贴、语音输入都要跟手，不能等 900ms。

顺带三处：

| 项 | 以前 | 现在 |
|---|---|---|
| 清除按钮 | `Router.go('/search')` 整页重建 | 只清空输入框并退回历史记录，不跳页、不丢筛选 |
| 排序按钮文字 | 未选排序时显示「排序」 | 显示「最佳匹配」，与 GitHub 一致 |
| 地址栏 `sort` | 空值不写入 | 无条件写入，切页签 / 点历史 / 刷新后排序不丢 |

### 同步：`bindHashLinks` 的调用对象

`loadPage()` 里写的是 `window.bindHashLinks(box)`，而 `box` 就是 `#sres` 本身。
这个函数接受容器元素、内部再查子节点，两条缓存还原路径也都是这么传的 —— 三处已统一成一致写法，
避免结果里的 `#/` 链接点不动。

**验证**：静态断言 8 项全过（清除按钮 / 搜索按钮 / 排序筛选条都改成无条件渲染 +
`syncToolbar()` 绑定 input + `doSearch` 内调用 + 清理按钮不再跳页 + 排序文字 +
`navQs` 带 sort）。

---
## 1.1.3（修：一键加群点了没反应）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 修：点「一键加群」弹「无法打开链接」

两层原因叠在一起：

**第一层：`openExternal` 给所有链接都加了 `CATEGORY_BROWSABLE`。**

```java
Intent i = new Intent(Intent.ACTION_VIEW, Uri.parse(url));
i.addCategory(Intent.CATEGORY_BROWSABLE);   // ← 就是这行
```

这个 category 是给「浏览器可安全打开的网页」用的过滤条件。加上它，系统只匹配
声明了自己能处理 browsable 的 Activity。而加群走的是 `mqqapi://` 这种自定义
scheme，只有 QQ 的 Activity 能接，它并**不声明** browsable —— 于是匹配结果为空，
`startActivity` 抛 `ActivityNotFoundException`，被 catch 成那句「无法打开链接」。

这个方法是给 http/https 外链写的，那些确实该带；用在自定义 scheme 上必然失败。
顺带一提，`mailto:`、`tel:` 走的是同一条路，同样打不开，一并修好。

**第二层：`mqqapi://card/show_pslcard` 这条老接口本身已经过时。**
就算过了上面那关，新版 QQ 上也经常被拒。

现在「一键加群」改走官方现行的 https 加群链接 `https://qm.qq.com/q/pFkpHXKCCk`：
装了 QQ 会直接跳到加群页，没装 QQ 就由浏览器打开网页版引导页。
群号 `806894257` 保留，「复制群号」那一路照旧能用。

配套的兜底：自定义 scheme 万一还是没人接（比如对应 App 没装），自动退化成浏览器
打开，绝不留下「点了没反应」。

---

## 1.1.3（修：中英混排翻不出来）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。
本包由**本公开库**编译产出。

### 修：中文里掺英文、英文里掺中文，整段都不翻

以前判断「这段是不是已经用目标语言写着」只数**占比**：目标文字占到三成五以上
就认定「不用翻了」。

问题是混排段落里，母语那部分照样过半：

| 段落 | 汉字占比 | 旧判定 | 用户想看到的 |
|---|---|---|---|
| `中文里掺 English 词，应该翻成中文` | 61% | 跳过 ❌ | 翻 |
| `仓库 githup 使用 WebView 加载前端` | 52% | 跳过 ❌ | 翻 |
| `这是中文描述，含 English words` | 37% | 跳过 ❌ | 翻 |
| `My World 我的世界开发者中文指南` | 61% | 跳过 ❌ | 翻 |

方向反过来也一样：翻成英文时，英文句子里夹着的中文同样被当成「已经是英文」跳过。

改成两步：占比不过半的照旧直接翻（英文页面走的就是这条快路径，没有额外开销）；
**占比过半的再扫一遍，看里面有没有连续成词的另一种文字** —— 有就翻。
连续两个字符才算「成词」，所以 `……只有一个字母 a ……` 这种零散字母仍然正确跳过，
纯母语段落也依旧不翻。

实测 14 条混排用例：修复前错 5 条，修复后全对；17 种语言的识别与
URL / SHA / 版本号 / 相对时间 / 语言名的跳过规则逐条比对无变化。

### 一并修：有道多返回一个空行，整批「翻不出来」

有道偶尔在译文的首尾各多给一个换行 —— 内容一行不少、顺序完全正确，
只是多了两个空串。以前拿它当「行数不符」处理，8 行进去 10 行回来就判失败，
整批一路对半拆到底后**放弃，原文原样上屏**。用户看到的就是「这一段死活翻不出来」。

现在先剥掉首尾空行再比行数，剥完仍对不上才拆（真错位的防御一点没放松）。
同一批实测：漏翻 24/24 → 0，请求数 35 → 14。

### 配套改动

前端资源变了，加固第 3 环的清单 `app/src/main/assets/guard/assets.sha`
已同步 `web/js/translate.js` 的新指纹（18 条全部复核一致）。

---

## 1.1.3（修：APK 下完装不上）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 修：下载完成的 APK 点安装，报「已阻止安装：校验失败：FileNotFoundException」

这个包**是完整的** —— 通知栏显示 743.4 KB，跟 Release 上的附件一分不差。
坏的从来不是包，是「去找这个包的路」。

下载由系统 `DownloadManager` 落到公共 `Download/githup/` 目录；而装包前的
校验却自己按 `Download/githup/ + 文件名` 拼出一条路径去读。这两件事在
**Android 10 之前**是同一个地方，之后就不是了：

Android 10 起引入分区存储，App 拼出来的这个路径指向的是**应用沙箱内的影子目录**，
里面空空如也，而真文件在系统那一边。于是 `!f.exists()`、
`FileNotFoundException` 抛出，最后落到 Toast 上就成了那句「校验失败」。

一句话：**路走错了，然后把「没找到」当成了「包有问题」。**

同一个错路径还害了另外两处，一并修掉：

| 位置 | 以前的表现 | 修法 |
|---|---|---|
| 装包前的 SHA 校验 | 完好无损的包被判成校验失败，拦下安装 | 改向 DownloadManager 要文件地址 |
| 下载管理里点「打开」 | 文件明明在，却提示「文件不在了」，点了没反应 | 存在性判断改走同一条查找逻辑 |
| 下载管理里点「删除」 | 显示「已删除」，文件还原封不动在原地 | 按下载 id 调 `DownloadManager.remove()`，并记录 id 备用 |

现在「找已下载的文件」统一走一个入口：**先问 DownloadManager**
（`getUriForDownloadedFile()` 在任何系统版本上都给得出可读地址），
问不到才退回老的公共目录路径给旧设备兜底。这条路是下载的发起方指认的，
不依赖我们对路径的任何假设。

顺带修掉的几件事：

- **报错不再吓人** —— 「读不到下载好的文件」不再写成「可能被篡改」。
  前者是环境问题，后者听起来像被人动了手脚，是两回事。
- `DownloadManager.enqueue()` 在外部存储没挂载、ROM 拒绝带子目录的目标路径等
  情况下会抛异常，以前会一路抛到 UI 线程，现在收住、记日志、换下一条路重试。
- 子目录建不起来时会退回 Download 根目录下载 —— 这种情况以前**必然**触发
  上面的 FileNotFound，现在因为位置是向 DownloadManager 问来的，不受影响。

### 顺带修的几处 null 崩溃

扫描这两个库时一并发现并修掉，都是「网络层偶尔解不出响应体」那一类：

| 位置 | 后果 |
|---|---|
| `page-detail.js` PR 的 checks 页 | PR 来自已删除的分支时 `head` 为空，整个 checks 页崩 |
| `page-repo.js` 仓库设置保存 | 保存成功了，但拿不到名字时会崩，还会误判成「已重命名」 |
| `page-repo.js` 新建议题 | 议题建好了，响应里没带编号就崩；现在改为回到议题列表 |
| `page-repo.js` 导入仓库 | 建仓库失败时拿 undefined 拼出畸形 URL 再失败一次，看不出真实原因 |
| `page-home.js` 我的议题/PR | 搜索无结果时崩 |

这些都是低概率分支，但真碰上了就是白屏。现在的处理是：要么给出明确错误，
要么优雅退化（比如议题已建好就回列表，让用户看到结果）。

---

## 1.1.3（多语言翻译：任意语言 → 中文或英文）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 新：不再只能翻英文

以前「能翻什么」是由一行代码决定的：

```javascript
if (!/[A-Za-z]/.test(s)) return false;   // 没有拉丁字母就不翻
```

日文、韩文、俄文、阿拉伯文、泰文、希腊文、希伯来文、印地语……
全都因为「一个 ABC 都没有」被判成不值得翻译，**从头到尾没进过翻译流程**。

所以瓶颈从来不在引擎 —— 引擎那边一直是 `auto` 自动检测，
多语种支持它们早就有。是这边的准入把它掐死了。

现在改成看**书写系统**，而不是看有没有 ABC：

| 目标语言设为 | 判定逻辑 |
|---|---|
| 简体中文 | 汉字占比不到 35% → 翻 |
| English | 拉丁字母占比不到 35% → 翻 |

写法上没有用一堆正则去遍历每个字符，而是一张 Unicode 区段表
加 `charCodeAt` 数值比较（前面还垫了一条 ASCII 快路径，
GitHub 上绝大多数文本走这条路就出结果了）。原因见末尾的性能一节。

菜单里多了一项 **「翻译成」**，可以在简体中文和 English 之间切。
**默认是简体中文**，老用户升级后看到的行为跟以前完全一样，不会突然变英文。
切换时会先把页面还原成原文再重翻一遍，不用手动关掉再开。

### 顺带修的三处

1. **MyMemory 的目标语言写死了** —— 那家接口不认 `auto`，
   当时直接填了 `zh-CN`。切到「翻成英文」时它还在往中文翻。现在两边都是活的：
   目标跟着设置走，源语言用上面那张区段表猜一个给它。
2. **缓存 key 没带目标语言** —— 同一段英文，翻成中文和翻成英文是两个结果。
   不带上目标语言的话，切过去会命中另一种语言的旧译文，看着像「没切换成功」。
   老 key（没有这一段）自然失效、慢慢被淘汰，不影响正确性。
3. **`/\W/` 的坑** —— 原本还有一句 `if (/^\W+$/) return false`，本意是「纯标点就不翻」。
   但 JS 的 `\W` 是 `[^A-Za-z0-9_]`，**中文、日文、俄文全都算 `\W`**：
   这句会把所有非拉丁文本当成纯符号跳过。以前它被前面那道
   `if (!/[A-Za-z]/)` 闸门挡着，走不到这儿，所以一直没出事；
   闸门拆掉当晚它就露出了獠牙。现在「有没有可识别文字」由区段表负责，这句已经删掉。

### 判不准的两个边界，以及怎么处理的

**日文**最麻烦，它大量借用汉字：

```
リポジトリの活動に関する説明です
```

这一句里汉字段落能占到三成七，光看汉字占比会判成「已经是中文」而跳过。
所以占比之外还得看**决定性特征**：只要出现假名，这段文字就是日文，
跟汉字占多少没关系。

**法文 / 西班牙文 / 越南文**这些跟英文共用拉丁字母，光看书写系统分不出来。
但它们普遍带附加符号（é ü ñ ç ş ộ ư），正经英文很少这么写。
于是加了条「带符号字母占比」的辅助判据，门槛按样本定在 **8%**：

- 法语、波兰语、越南语一般落在 8~30% → 判定为「不是英文」，翻
- `Björn`、`Pokémon` 这类混在英文里的外来词约 3.8% → 稳稳留在原地，不乱翻

**已知限制**（说在前面）：德语、西班牙语的重音实在太稀疏（2~3%），
够不到 8% 这条线，**翻成英文时会被当成英文跳过**。翻成中文不受影响。
取舍是这样的 —— 真判断错了只是多翻一句；漏判会让整篇外语 README 停在原地，后者更亏。
想翻它们的话，手动「重新翻译」即可。

实测在这次做的 17 语种样本上（同一份页面，两个目标各跑一遍）：

| | 送去翻译 | 正确跳过 |
|---|---|---|
| 翻译成简体中文 | 英文、日文、韩文、俄文、阿拉伯文、泰文、希腊文、希伯来文、印地语、法文、德文、西班牙文、葡萄牙文、越南文、土耳其文、波兰文 | 中文 + URL / SHA / 版本号 / 相对时间 / @提及 / 语言名 |
| 翻译成 English | 中文、日文、韩文、俄文、阿拉伯文、泰文、希腊文、希伯来文、印地语、法文、葡萄牙文、越南文、土耳其文 | 英文（及上面那条已知限制里的三种）+ 同样的 URL / SHA 等 |

URL、commit SHA、版本号、`3 days ago`、`@someone`、`TypeScript`
这些不该翻的，两种目标下都仍然跳得干干净净。

### 性能：49 ms → 11 ms

书写系统判定第一版是用正则写的，每个字符要跑十来个状态机。
探索页一屏几百个候选、滚起来上千个，账就这么堆起来了：
600 行的页面单轮 **49 ms**，比上一版优化完的 9 ms 倒退了五倍。

改成 `charCodeAt` + 数值区间比较之后回到 **11 ms**，
而且在 `needTranslate` 里只扫一次，结果给两个判断共用。

上一版修掉的那些账也一笔没丢：序列化仍是 **1 次**、跨原生桥仍是 **1 次**、数据量 **0.02 MB**，
「缓存越大越慢」没有回来。

---

## 1.1.3（修翻译越用越慢）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 修：翻译越来越慢

不是错觉，是真的会随使用时长线性变慢。量出来的账在这儿（一轮翻 37 段）：

| 时机 | 序列化次数 | 磁盘写入 | 跨原生桥 | 序列化数据量 |
|---|---|---|---|---|
| 刚装好（缓存空） | 74 | 77 | 37 | 0.20 MB |
| 攒到 300 条 | 74 | 78 | 37 | 1.83 MB |
| 攒到上限 600 条 | 74 | 78 | 37 | **2.45 MB** |

根子在 `cacheSet`：**每译出一段，就把整份缓存重新序列化一遍落盘一次**。
一轮几十段就是几十次全量洗一遍，而缓存只增不减，于是用得越久、每一段越贵。

具体的三笔浪费：

1. **每段一次全表操作** —— `Object.keys(整份缓存)` 全扫一遍只为数个数，
   然后 `JSON.stringify(整份缓存)` 再扫一遍，最后 `SharedPreferences.apply()` 提交一次。
2. **同一份数据写了两遍** —— `prefSet` 里跑完 `Store.setJSON`（内部已经桥 + localStorage
   双写）之后，末尾又补了一次 `localStorage.setItem`，等于整套再付一遍。
3. **三条轮询链各自扫全树** —— `watchView`(900ms)、`catchUp`(1200ms)、`retryLoop`(4000ms)
   互不知情，常挤在同一两百毫秒里把整棵子树各扫一遍。
   `collect` 里还有一处：`inSkip()` 内部是 `p.closest(SKIP_SEL)`，同一父元素下七八个
   节点就把那串选择器重复解析七八遍。

改法都落在 IO 和调度上，**翻译引擎选择、降级顺序、上屏结果、缓存内容一律没动**：

- 译文写内存即时生效，落盘合并到一轮结束后的一次（换页 / 切后台前强制补一次，不丢译文）
- `prefSet` 去掉重复的那次双写
- 轮询用的「有没有新内容」加 250ms 结果复用窗；真正开翻时（`translatePage`）先作废旧答案重扫，拿的一定是最新 DOM
- `inSkip` 结果按父元素缓存，和已有的 `rects` 缓存一个道理
- `needTranslate` 里最贵的那个几十分支的语言名正则，先按长度剪一刀再喂

改完再量同一轮：

| 时机 | 序列化次数 | 磁盘写入 | 跨原生桥 | 序列化数据量 | 耗时 |
|---|---|---|---|---|---|
| 刚装好（缓存空） | **1** | 4 | **1** | 0.00 MB | 5 → 3 ms |
| 攒到上限 600 条 | **1** | 5 | **1** | **0.02 MB** | 8 → 1 ms |

页面节点从 60 行滚到 600 行时，单轮 16 → 9 ms。

关键是曲线形状变了：**以前是「缓存越大越慢」，现在几乎与缓存规模无关**。

等价性对照（同一份页面跑新旧两个版本）：上屏译文逐条一致、落盘缓存逐字节一致、
第二轮命中率一致、重启后呈现一致 —— 五项全过。

---

## 1.1.3（修首页/我的页的 null 崩溃）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 修：首页/我的页 `Cannot read properties of null (reading 'login')`

网络层偶尔解不出响应体，`r.data` 就是 `null`。页面拿它当对象用，读 `.login`
当场抛异常，整页只剩一句报错。**出错的位置在两处，上次只补了一处。**

具体是这些地方在裸奔：

| 位置 | 干什么 | 后果 |
|---|---|---|
| `page-home.js` 首页动态 | `/user` 的 data 为 null 时先写进 `Session.user`，再读 `me.login` | **把整站登录态一起带坏**，页面崩 |
| `page-user.js` 我的页 | `paint(r.data)` 直接传 null，paint 第一行读 `u.login` | 崩 |
| `page-user.js` 资料保存 | 保存成功后拿 null 覆盖用户对象 | 崩，还看不到「已更新」 |
| `page-user.js` 用户页/Gist | `r.data` 直接当对象用 | 崩 |

首页那处最要命：它先把 null 写进 `Session.user`，等于**污染了全局登录态** ——
之后任何读 `Session.user.login` 的地方都会跟着挂。

**改法分两层：**

1. **`api.js` 层治本** —— 正常响应一定带体（204/304 除外），解出来是 `null`
   说明是真的故障，不是「结果就是空」。现在直接当成请求失败抛出去，
   让各页已有的 `.catch()` 去展示错误。以前是把 null 交给页面自己撞。
2. **各页兜住** —— 拿到用户对象才渲染；拿不到就用已有的登录态顶一下；
   连那个都没有才显示错误。无论如何不让 `null` 流进渲染代码。

实测对照（把仓库里真实的 `page-home.js` 拉到 Node 跑，模拟 `/user` 响应体丢失）：

| | 修复前 | 修复后 |
|---|---|---|
| 有登录态可兜底 | ❌ 页面显示 `Cannot read...` 报错 | ✅ 用已有登录态正常渲染出用户信息 |
| 冷启动无兜底 | ⚠️ 报错（不该白屏） | ✅ 显示「用户信息读取失败」，页面可用 |

### 为什么会漏

上次修的是 `app.js` **启动时**读用户那处，判断「首页崩的应该是这里」。实际上
`page-home.js` 里还有一条**运行时**路径（进首页拉 `/user`），同一个模式没被覆盖到。

这次是把「`= r.data` 之后解引用」这个模式在整个 assets 目录全量扫了一遍，
一共 4 处，都补上了，不再逐个猜。


## 1.1.3（改版本号变成一个命令）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 新增：`set-version.sh` —— 改版本号一键完成

以前改版本号是四步人肉操作：

1. 改 `version.lock`
2. 改 `app/build.gradle`
3. 重跑 `tools/gen-guard.py`（防护链常量要跟着变）
4. 更新 `version.json`

第 3 步漏掉最要命 —— 包内版本是 1.1.4、防护链常量还写着 1.1.3，启动就被官方校验拦下，
弹「版本号被改成了 1.1.4」。1.1.4 那次事故就是这么炸的。

现在一句话搞定：

```bash
bash set-version.sh 1.1.4            # 内部号按版本号换算
bash set-version.sh 1.1.4 1001006    # 内部号显式指定（版本号往回调时必须）
```

四步全在里面，跑完**当场自检**，把五处值拉出来对一遍：

```
自检：
  ✅ version.lock           1.1.4
  ✅ build.gradle           1.1.4
  ✅ version.json           1.1.4
  ✅ 前端 APP_VERSION     1.1.4
  ✅ 防护链 VERSION_NAME 1.1.4
  ✅ 防护链 VERSION_CODE 1001004
```

任何一处不一致就红字报错、非零退出，**不会给你一个「看着改完了其实漏了」的状态**。

实测跑过一遍完整来回（1.1.3 → 1.1.4 → 1.1.3）：
切到 1.1.4 后打包，包内 `versionName=1.1.4 / versionCode=1001004`，
`GuardKeys` 里同样是这两个值 —— **两边一致，启动不会被拦**。

那把锁还在吗？在。`set-version.sh` 是**唯一**被允许改 `version.lock` 的入口，
`build-apk.sh` 依旧拒绝任何版本不符的打包。锁防的是「顺手改」，不防「我明确要发新版」。

### 改：`release.sh` 修好并接管全流程

- 修掉硬编码 `github-mobile` 子目录的 bug（目录其实不存在，原脚本一跑就错）
- 第 1 步改成调用 `set-version.sh` —— 发版时版本号与防护链必然同步
- 不再自己拼 `version.json`（交给打包脚本，见下），避免两处写法打架

现在发一个版本就是：

```bash
bash release.sh 1.1.4 "说明"
```

改版本 → 打包 → 回填指纹 → 提交 → 打 tag，一条命令到底。

### 改：`build-apk.sh` 打包后自动回填 size / sha256

更新检测是拿 `version.json` 里的指纹跟「设备上已装包的哈希」比的。包换了、清单没换，
用户就会被反复提示更新（或者反过来，永远提示不了）。现在打包顺手回填，指纹永远跟着包走。


## 1.1.3（版本号锁定为 1.1.3）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 改：版本号锁死 1.1.3，谁也改不动了

起因是 1.1.4 那次事故 —— 版本号被顺手改成 1.1.4，而防护链常量（`GuardKeys`）还停在 1.1.3，
烙进包里的身份和包内实际版本对不上，装上去直接被官方校验拦下（提示「版本号被改过」）。

现在版本号由一个独立文件 **`version.lock`** 钉死（`version=1.1.3` / `versionCode=1001003`），
三道闸：

| 闸 | 拦什么 |
|---|---|
| `app/build.gradle` | 构建时读 `version.lock` 比对，对不上直接构建失败 |
| `build-apk.sh` / `release.sh` | 传别的版本号（或别的 versionCode）直接拒绝 |
| `tools/gen-guard.py` | 防护链常量自动从 `build.gradle` 读，不手写（事故后已改） |

也就是说，**改版本号变成了一个必须先动 `version.lock` 的显式动作**，顺手改不动了：

1. 改 `version.lock` 的 `version` / `versionCode`
2. 改 `app/build.gradle` 里对应的两个值
3. 重跑 `tools/gen-guard.py`（防护链常量要跟着变，否则启动被拦）
4. 更新 `version.json`

实测过四种情况：

| 操作 | 结果 |
|---|---|
| `bash build-apk.sh 1.1.4` | ❌ 拒绝：版本号已锁定，不接受 1.1.4 |
| `bash build-apk.sh 1.1.3 1.1.3 1001005` | ❌ 拒绝：versionCode 已锁定 |
| 手动把 `build.gradle` 改成 1.1.4 | ❌ 构建失败，报「版本锁定」 |
| 正常打包 1.1.3 | ✅ 通过，产物哈希与加锁前完全一致 |

最后一行是关键：产物 SHA-256 与加锁前一模一样，说明这道锁**只做校验，没碰任何算法**。
防护链、加密、签名、网络层、上一轮的搜索修复都保持原样。

### 删：v1.1.4 这个 Release

它是那次事故留下来的坏包：装上去会被官方校验拦下，而且内部号 `1001005` 比 `1001003` 大，
Android 不给「倒退」的包做覆盖安装 —— 留着就是让别人装不上 1.1.3。已经删掉，
现在仓库里只剩 v1.1.3 一个正式版。


## 1.1.3（修复搜索后结果不刷新）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 修：搜完不显示，得切一下页签才出来

搜了关键词，界面一直停在骨架屏。把「仓库」切到「用户」再切回来，结果才出来。

原因是结果回来时多做了一次「这次数据还算不算数」的判断，而那次判断比的是地址栏：

```js
if ((location.hash || '') !== '#/search?' + myQs) return;
```

`myQs` 是 `navQs()` 拼出来的目标串，**恒定带 `type`**；而在输入框里打字、按回车、
等节流自动搜，走的都是就地 `doSearch()` —— **页面不跳转，地址栏一动不动**。

于是 `#/search` 永远 != `#/search?q=github&type=repositories`，结果整批被判成
「过期」丢掉了。切页签之所以管用，是因为只有那一步才会去更新地址栏。

顺带一提，从历史记录点进 `#/search?q=github`（不带 type）也是同一个毛病。

改法是别再拿地址栏当依据 —— 改用「渲染实例」：每进一次搜索页发一个新身份，
结果回来对一下身份，页面被换走了才丢弃，同一次浏览里怎么搜都正常。
换关键词导致的过期另有票据（`loadSeq`）管着，两边互不影响。

已经把仓库里真实的 `page-home.js` 拉到 Node 里跑过对照：
修复前两条路径都卡在骨架屏，修复后都立刻渲染出结果。

### 顺带：优化这部分可能拖慢体感的地方也复查了

- **`debounce` 900ms** 是刻意留的：GitHub 搜索一次要好几秒，边打字边搜会把并发占满，越打越慢
- **至少 3 个字才自动搜**也是刻意的，两个字命中太宽
- **想立刻搜**：直接按回车或点「搜索」按钮，不用等节流
- **5 分钟搜索缓存**按完整 URL（含关键词）为键，不同关键词不会串味；同一关键词来回切页签是瞬时的


## 1.1.3（修复首页报错 Cannot read properties of null）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），可直接覆盖安装。

### 修：一进首页就弹 `Cannot read properties of null (reading 'login')`

这是**上一版「网络性能优化」带进来的回归**。优化上线时把 `Accept-Encoding` 从
`identity` 改成了 `gzip`，却漏了和 `Transfer-Encoding: chunked` 的组合 ——
GitHub 在没有明确 `Content-Length` 时默认就用这一组合，所以几乎全站受影响。

两种编码叠在一起，**处理顺序是有讲究的**：chunked 的分块尺寸写在明文里，
得先按块把数据剥出来，剥出来的原始字节才是 gzip 流。原来的写法刚好反了，
先把裸 socket 交给 `GZIPInputStream`，它读到 `4d\r\n` 这类十六进制长度行，
第一步校验 gzip 魔数就失败：

```
java.util.zip.ZipException: Not in GZIP format
    at com.hubmobile.app.Http.readResponse(Http.java:591)
```

响应体丢失 → 前端拿到 null → `r.data.login` 直接崩 → 整页只剩一句报错。
和 [议题 #4](https://github.com/Buwrt/githup/issues/4) 报的错一模一样，但成因不同。

改法是先把 `readChunked()` 的结果（原始字节）交给 `GZIPInputStream` 解压。
同样的坑有两处，都改了：

| 位置 | 影响范围 |
|---|---|
| `execBytes()` | 所有 API 请求（首页崩溃就是它） |
| `readResponse()` | 上传（`requestMultipart()`），另一段读取逻辑里的同一个错 |

验证方式：把仓库里真实的 `Http.java` 拉到 JVM 上跑，本机 mock 出 chunked+gzip
响应 —— 修复前必抛 `ZipException`，修复后两条路径都能解出完整 JSON。

### 改：单点解析失败不再拖垮整个首页

「某一环出错 → body 变 null → 页面白屏」这条链太脆。`app.js` 里登录态读取
加了兜底：解析不出用户对象就按未登录继续启动。往后类似故障最多让某个功能
不可用，不会再整页打不开。


## 1.1.3（网络性能优化）— 2026-09-19

加载变慢的根因在**原生网络层**，不在页面本身：

1. **每次请求都重建 TLS 连接** —— 写死 `Connection: close`，一次握手移动网下 300~800ms，
   列表页并发几个请求就付几次。现在按 host 复用（keep-alive），上限 6 条、空闲 60 秒回收
2. **响应不压缩** —— `Accept-Encoding: identity` 写死，搜索响应全裸传输；改 gzip 后降到 1/4~1/5
3. **逐字节读 HTTP 头** —— readLine 怼裸 socket；加 16KB 缓冲，并修掉尾部漏传长度的老问题
4. **搜索零缓存** —— 切页签重打同一批数据；现在默认缓存 5 分钟

另外：搜索输入节流 500ms → 900ms，加请求票据防慢响应覆盖新结果，启动时预热 DNS。


## 1.1.3（Star 列表排序与收藏夹）— 2026-09-19

文件名 `githup-1.1.3.apk`，对外版本号 **1.1.3**（versionCode `1001003`）。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`）。

> **注意**：内部号从 1001005 回调到 1001003（两个库统一）。
> 手机不会给「内部号更小」的包做覆盖安装 —— 已装过 1001005 那版的设备需要先卸载。

### 新增：我的 Star 有了列表、排序和收藏夹

议题 [#3](https://github.com/Buwrt/githup/issues/3)。

- **排序**：Star 时间 / 最近更新 / Star 数三种
- **Star 时间**：带 `Accept: application/vnd.github.star+json` 取 `starred_at`，列表显示「Star 于 …」
- **收藏夹**：条目可收藏 / 取消，顶部可只看收藏夹
- **筛选**：按仓库名 / 描述即时过滤


## 1.1.3（相册视频 / 搜索筛选 / PR 评审）— 2026-09-18

文件名 `githup-1.1.3.apk`，包内版本号 **1.1.3**（versionCode `1001003`），Android 7.0 起可用。
签名与上一版相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），**直接覆盖安装，不用卸载**。

这一版补三件事：相册里的**视频**终于能选了、搜索有了**排序和筛选器**、
拉取请求可以**评审和合并**了。

---

### 新增：搜索结果可以排序、可以筛

网页端搜索框右边有一排筛选器，App 里一直没有 —— 现在补上了。搜索结果页顶部多了一条：

- **排序**：点一下选排序方式（仓库有 Star 数、Fork 数、更新时间等；议题有创建时间、评论数、点赞数等）
- **筛选**：点「筛选」打开面板，可选**语言 / Star 数 / Fork 数 / 最后推送时间 / 开源许可 / 是否已归档**，
  选完在面板底部能直接看到拼出来的搜索式，确认无误再应用
- 生效后按钮上会显示条数，结果区上方标出当前排序和**实际发给 GitHub 的搜索串**（便于核对）
- 「清除筛选」一键回到原始关键词

两处讲究：

- GitHub 只有**仓库 / 议题 / 提交**这三类搜索支持排序参数，用户、代码、话题不支持 ——
  这三类不显示排序入口，也不给接口传 sort，否则直接报 422
- 筛选不是另传参数，而是拼成 GitHub 自己的限定符（`language:`、`stars:>=`、
  `pushed:>=日期`…）。如果你已经在搜索框里手敲了同名限定符，会以面板里选的为准，不会两个打架
- 「最近一周」这类相对时间会被换算成**具体日期**，因为 GitHub 只认日期不认 `7d`

---

### 新增：拉取请求可以评审、可以合并

以前在 App 里看 PR 只能评论和关掉，改不动评审状态。现在：

**评审**（右上角菜单 → 评审，PR 处于开启状态时才有）

- 三种结论：**批准** / **请求修改** / **仅评论**
- 可以写总体说明，也可以什么都不写（仅评论时至少要写点什么）
- **行内评论**：在「文件改动」里点任意一行，就能针对那一行写意见。
  意见不会立刻发出去 —— 会先攒着，随你提交的评审一起发给对方，跟网页端一致
- 会话页顶部能直接看到**别人已经提交的评审**：谁批准了、谁要求改

**合并**（右上角菜单 → 合并）

- 三种方式：**创建合并提交 / 压缩合并 / 变基合并**，可改提交标题和描述
- 合并前先告诉你能不能合：冲突、被分支保护挡住、分支落后、检查未通过，各有各的提示；
  真处于冲突或被保护状态时会再确认一次，不让你白点
- 草稿、已关闭的 PR 直接拦下
- **合并后可自动删除源分支**（默认勾上）。两个安全限制：
  跨仓库 fork 过来的分支不给删（那不是你的分支），目标仓库的默认分支也不给删
- 删分支失败不算合并失败 —— 合并已经成了，只是提示一句「分支删不掉」

---

### 修：相册里的视频选不了

上一版把插图改成走系统相册后，图片能选、**视频却要进文件夹里翻**。
原因是我自己写窄了：同时要图片和视频时直接跳过了相册，退回文件浏览器。

系统相册本来就支持「照片 + 视频」一起显示 —— 只要**不限制 MIME 类型**即可。
现在按你要的类型分流：

| 你要什么 | 打开哪里 |
| --- | --- |
| 只要图片 | 相册（只显示照片） |
| 只要视频 | 相册（只显示视频） |
| 图片 + 视频 | 相册（照片和视频一起显示） |
| APK / 压缩包等 | 文件浏览器 |

---

---

## 1.1.3 — 2026-09-17

文件名 `githup-1.1.3.apk`，包内版本号 **1.1.3**（versionCode `1001003`），Android 7.0 起可用。
签名与 1.1.2 相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），**直接覆盖安装，不用卸载**。

### 新增：翻译本页

浏览器上的 GitHub 能用扩展翻译，App 里的 WebView 装不了扩展 —— 所以把翻译做进了 App 自己。

- 右上角**地球图标是总开关**，默认关闭。打开后当前页翻成中文，关掉即还原原文
- **总开关是全局的**：只要不关，换到哪个页面都接着翻，不用每页重新点
- **滚动驱动**：只翻屏幕上下各 600px 范围内的英文段，滚到哪译到哪，屏幕外不翻
- 自动跳过代码、仓库名、用户名、`@某人`、版本号、文件路径
- 译文缓存在本机（上限 600 条），同样的内容不重复请求；菜单可一键清空
- 还原时不重建页面、不重新请求接口

全部免费、开箱即用，不用注册不用 Key，默认**自动探测**谁最快。设置页可手动指定引擎，
长按翻译图标进菜单可切换引擎 / 清缓存。

| 引擎 | 特点 |
| --- | --- |
| 有道 | 国内直连最稳的免费引擎，无需 Key |
| 设备端翻译 | 系统内置，**离线、不联网、文本不出设备**（需装中英文语言包） |
| DeepL | 质量最佳，公共 IP 偶尔限流 |
| Google | 老牌免费接口，需海外网络 |
| MyMemory | 兜底，全球可达，匿名有日配额 |
| ~~微软 Edge~~ | **已停用**：微软关闭了公开令牌接口，实现保留但已移出探测链 |

引擎挂了自动换下一家；某几段没翻出来会隔 4 秒自动重翻，最多 5 轮。

### 新增：平板 / 折叠屏适配

- 不再锁死竖屏：平板横持、分屏、折叠屏展开都跟随系统旋转
- 转屏**不重建 Activity** —— 登录态、翻译开关、滚动位置都不丢
- 宽屏下内容限宽居中（900px），底部标签栏不再被拉成 1280px 平分
- 横屏时左右避开刘海 / 挖孔

### 修复

1. **打开翻译后页面加载变慢**（骨架屏一直转）
   翻译把网络线程池占满了：页面数据和翻译请求共用 4 个线程的池，自动探测时连不上的引擎
   要挂满 20 秒连接超时，而且失败没有记忆、每换一页重来一遍。
   现在：线程池 4 → 8，连接超时 20s → 12s，探测失败进 10 分钟黑名单 + 全败后 2 分钟冷却。
2. **翻译请求在真机上一次都没走原生通道**
   链路是两层（`translate.js` → `window.Native.http`（api.js，收对象）→ `NativeBridge.http`（Java，收字符串）），
   之前多 stringify 了一层，Java 侧 `new JSONObject` 抛异常 → 请求 `status 0` 失败 → 只能退回 fetch 兜底。
   表现就是「只有 MyMemory 和有道能用、Google / DeepL 全灭」，而且每个请求都要失败一次再重试。
   现在只转换一层，原生通道恢复正常。
3. **翻译拖慢、翻一半就停**（1.1.3 之前的遗留问题一并修掉）
   有道接口有频率限制，原来每段一个请求必然撞 `411`；现在换行拼批（≤8 行 / 800 字符）、
   相邻请求间隔 ≥800ms，失败时对半拆分重试而不是退成逐条。
4. **搜索结果只显示第一页，翻不下去**（议题 #1）
   搜索只请求了第 1 页 `per_page=30`，既没传 `page` 也没有翻页入口，于是不管有多少结果都只有那一屏。
   现在底部有「加载更多」，表头显示「共 N 条结果（已加载 M 条）」；
   GitHub 搜索本身最多给 1000 条，到顶会明确提示，不再让人以为软件坏了。
5. **搜索结果点进详情再返回，会重新加载并回到顶部**（议题 #2）
   路由渲染时无条件 `innerHTML=''` + `scrollTop=0`，回退也照做。
   现在按路由记住滚动位置，返回时原样还原；搜索结果按「类型 + 关键词」缓存，返回不再重新请求接口。
6. **issue / README 里的视频和图片看不了**
   视频：Markdown 渲染器把所有原始 HTML 一律丢弃，而 GitHub 上传的视频就是裸的 `<video>` 标签 —— 连影子都没有。
   现在放行一小撮安全的排版/媒体标签（仍有 DOMPurify 兜底），裸的 `.mp4/.mov/.webm/.m4v` 链接也会就地变成播放器，
   并补上 `controls`（GitHub 贴进来的标签未必自带，缺了就是一块按不动的黑砖）；全屏播放和返回键退出全屏也接上了。
   图片：外链图片被包成 `<a target="_blank">` 且不带 `data-zoom`，而 WebView 没开多窗口 —— 点下去既不放大也不跳转。
   现在内外链图片一律可点开，走内置查看器；加载失败的给一个可见的虚线框，不再只剩一片空白。
7. **议题评论里上传的视频 / 截图点开还是看不了**（1.1.3 首发后的补充修复）
   拖拽上传到 GitHub 的附件贴出来就是一行 `…/user-attachments/assets/<uuid>`，**没有扩展名**，
   而上一版的识别靠 `.mp4 / .mov` 后缀，这类链接直接漏掉。更根本的是：
   Markdown 渲染器会把正文里 7~40 位的十六进制串自动链接成 commit，而附件 UUID 恰好全是
   十六进制字符 —— URL 在识别之前就被撕成了「半个链接 + 一个假 commit」。
   现在：SHA 前面是 URL 成分（`/ - . =` 等）的一律不碰；无扩展名附件乐观渲染成播放器
   （只拉 metadata），加载失败自动降级 **视频 → 图片（可点开）→「📎 打开附件」链接**。
   裸的图片链接（`…/xxx.png`）现在也就地显示、可点开。

### 变更：更新规则（前两位变 = 强制）

版本号按 `x.y.z` 三段走，从高位往下比，「第一个出现差异的那一位」决定这次更新是强制还是可选：

| 变化 | 例子 | 行为 |
| --- | --- | --- |
| 第一位（大版本） | `1.1.1` → `2.0.0` | **强制更新**，弹层不可关闭，装好才能继续用 |
| 第二位（功能更新） | `1.1.1` → `1.2.0` | **强制更新**，同上 —— 这条是本次改的，以前是可更可不更 |
| 第三位（修复更新） | `1.1.1` → `1.1.2` | **可选更新**，给「立即更新 / 稍后提醒 / 跳过此版」 |
| 版本号没变、包换了 | `1.1.1` → `1.1.1` | **可选更新**，按安装包指纹（SHA-256）识别，标题写「有新内容可用」 |

**一句话：想让老用户必须升级就把第二位 +1，只是修 bug 就动第三位。**

强制更新时不认「跳过此版」—— 就算用户之前跳过过这个版本号，前两位一变照样拦。

### 修：通知删了，下次进来还在

「删除」以前调的是 `PATCH /notifications/threads/{id}` —— GitHub 根本没有这个方法
（通知线程只认 `PUT` 标记已读和 `DELETE` 标记完成），一律 404；404 又被 catch 吞掉，
界面只是把那几行从列表里移走，服务端一动没动 —— 所以看起来删了，下次进来全部回来。
「打开通知时标记已读」用的也是这个不存在的 PATCH，同样一直没生效。

现在：

- **删除 = `DELETE /notifications/threads/{id}`**（标记为 done，跟网页版点 Done 同一个动作），
  从收件箱里彻底消失，「未读 / 已读 / 全部」任何视图都不会再出现
- 打开通知 = `PUT`（标记已读），这次是真的会标上
- 已 done 的通知再删一次会回 404，按成功算，不误报失败
- 弹窗文案同步更正 —— 之前那句「GitHub 只提供标记已读，没有彻底删除的接口」是错的

### 修：翻译开始了就停不下来

翻译一启动，右上角地球图标就点不动了 —— 只能干等整页翻完，或者等 45 秒看门狗超时，
表现就是「暂停不了」。长文章尤其难受，几十段英文要一路翻到底。

原因在样式：`.tr-btn.busy` 上写了 `pointer-events: none`，本意是防止翻译途中重复点开，
结果连「关掉翻译」这条路也一起堵死了。停止逻辑本身一直是对的
（`restorePage()` 会 `state.seq++` 作废进行中的批次，迟到的译文也不会再写回页面）。

现在：

- 去掉 `pointer-events: none`，busy 只保留视觉反馈（图标缓慢旋转 + 略降透明度）
- 防止重复点开交给 `translatePage()` 里的 `state.busy` 拦，不需要在 CSS 层挡
- 途中叫停会提示「**已停止翻译，已翻的内容已还原**」，跟本来就没在翻时的
  「翻译已关闭」区分开，点下去立刻有反馈

### 变更：下载统一存到 `Download/githup/`

以前下载的东西直接扔在 `Download` 根目录，跟浏览器、微信、QQ 下的混在一起，找个文件得翻半天。

现在所有下载 —— 仓库 ZIP、Release 资产、Actions 构建产物、App 更新包，
以及 WebView 里点链接触发的下载 —— 统一落到 **`/storage/emulated/0/Download/githup/`**。

- 落盘位置只有一处定义（`JsBridge.downloadSubPath`），两个下载入口共用，不会再出现
  「有的存这儿有的存那儿」
- Android 10 起是分区存储，App 自己建不了公共目录，交给 `DownloadManager`（系统组件）建
- 子目录万一建不起来（个别 ROM），自动退回 `Download` 根目录重试 —— 位置不对也比下不到强
- 普通文件下载完成会提示保存位置；APK 照旧直接拉起安装器

### 修：README 里的图片看不了

README 里带图片的仓库，图片位置只剩一个空白框。

原因：README 里的图片基本都写成相对路径（`![图](docs/img/a.png)`）。网页上浏览器会拿
当前页地址去补全；App 里页面是 `file:///android_asset/web/index.html`，补出来是
`file:///android_asset/web/docs/img/a.png` —— 本地压根没这个文件，于是全部裂开。

现在按 GitHub 的规则自己补全成 raw 地址：

- 相对路径 → `raw.githubusercontent.com/{仓库}/{分支}/{README 所在目录}/{路径}`
  （`/` 开头算仓库根，`../` 正常上跳，中文与空格做转义）
- `github.com/{o}/{r}/blob/{ref}/x.png` 这种写法也转成 raw —— 它直接当 `src`
  拉到的是一个 HTML 页面，同样是裂图
- README 在子目录（`docs/README.md`）时按子目录算，分支按你正在看的那个分支
- 内联写的 `<img src="a.png">` 同样生效；外链、`data:`、协议相对地址不动

### 修：图片还是裂 —— 加载也改走原生通道

上一版把地址补全成 raw 之后，不少网络下图片照样裂：
`raw.githubusercontent.com` 直连不通，而同网络的 `api.github.com` 是通的 ——
因为 App 里的列表 / README 文本走的是**原生网络栈**，WebView 自己发的请求走的是另一条路。

所以加载也交给原生：

- 原生按字节把图片拉回来，Base64 传给前端，转成 `data:` 塞回 `<img>`
  （二进制绝不过一遍字符串，UTF-8 解码会把图片搅碎）
- 队列并发 4，不会一次几十张图把线程池占满
- 令牌**只发给 GitHub 自家域名**，外链图床一个字都不带
- 拉不到（超时 / 404 / 超过 15MB）就保持原地址，交给裂图样式兜底，不会变空白

### 新增：App 内的下载进度条

以前点完「下载」App 里就没下文了，进度只在通知栏 —— 不看通知根本不知道在传、传了多少。

- 底栏上方一条进度条：文件名 + 百分比，多个任务显示「等 N 个」
- 拿不到总大小时来回扫（不是 0% 装死），失败变红打叉，结束自动收起
- 仓库 ZIP、Release 资产、App 更新包、WebView 里点链接触发的下载都算在内

### 已知问题

- **国内手机上通常只有有道和 MyMemory 能用**。Google / DeepL 需要海外网络，这是网络环境决定的，
  不是软件的毛病；微软 Edge 是厂商关停（公开令牌接口已返回 404）。
- 设备端翻译要系统装了中英文语言包才可用，没有就自动跳过。
- 翻译会把**被翻译的文本**发给所选服务商。私有仓库内容介意的话，把引擎固定到「设备端翻译」。

### 校验

- 大小：723,003 字节
- SHA-256：`2fb4601fa4ec1ad31fadd7e375d13ba96fbeff9998075823cb1a4b32f9f9b06e`

```bash
apksigner verify --print-certs githup-1.1.3.apk
sha256sum githup-1.1.3.apk
```

---

## 1.1.3（追加：改成在相册里选图）

### 改
- **插图直接在系统相册里挑**，不再进文件管理器。
  上一版为绕开空列表改用了文件浏览器 —— 照片能选到了，但那是文件管理器的样子：
  一屏 DCIM / Download / Android / Movies 文件夹名，要自己点进去翻。
  现在按类型分流：
  - 图片 / 视频 → 系统相册（Android 13+ 是系统照片选择器；7~12 是 ACTION_PICK + MediaStore）
  - APK / 压缩包 / 任意文件 → 文件浏览器（不变）
  相册路径三级降级，任何一个不可用就退下一级，最后兜底才是文件浏览器。
- 补 `READ_MEDIA_VISUAL_USER_SELECTED`（Android 14 的「仅允许选中的照片」）：
  用户选了这一项，相册里依然有内容，不会再变成空白页；
  已拿到该权限时不重复弹权限框。

---

## 1.1.3（2026-09-18 追加：附件上传）

同一版本号**不改**，只补功能：评论框与新建 Issue 的正文可以插入图片 / 视频了。

### 新增
- **写评论、提 Issue 可以插图录屏**（之前这两处只能写纯文字，没有插图入口）
  工具栏新增图片按钮（「引用」旁边），点击选图 / 选视频 → 自动上传到 GitHub →
  上传结果插到光标位置：图片用 `![](链接)`，视频贴裸链接
- 上传中按钮禁用并显示「上传中」；失败给具体原因（未登录 / 超过 25MB / 拿不到策略）

### 实现
- 新增 `js/upload-attach.js`：走 GitHub 官方三步 —— 申请上传策略
  （`POST /upload/policies/assets`）→ multipart 上传（**file 字段必须排最后**，
  否则 S3 拒收）→ 用 `asset.href` 组装最终链接（`user-attachments/assets/<uuid>`，
  **无扩展名**，由 md.js 的「视频→图片→链接」降级链渲染）
- 原生侧新增「流式 multipart」：`Http.requestMultipart` +
  `JsBridge.uploadMultipart`，用 `SequenceInputStream` 把「头 + 文件 + 尾」
  串起来 8KB 一块发送 —— 25MB 视频也不会整个读进内存
- `FilePick.open / sizeOf`：拿到 Uri 的输入流和大小
- `api.js`：`Native.uploadMultipart`（JS 只拼头尾，文件走原生通道）
- 兜底版本号常量 `APP_VERSION` 从 1.1.2 同步成 1.1.3（避免被更新检测误判成旧版本）

### 注意
- 前端多了一个文件，资源指纹变了 → **打包前必须重跑 `tools/gen-guard.py`**，
  否则第 3 环会因为哈希对不上把自己拦下来。已重签。

---

## 以后新增一个版本时

复制下面这段放到 `## 1.1.3` 那一节的**上面**，改掉版本号、日期和条目即可：

```markdown
## x.y.z — 日期

文件名 `githup-x.y.z.apk`，包内版本号 **x.y.z**（versionCode `...`）。

### 新增
- ……

### 修复
- ……

### 已知问题
- ……

### 校验
- 大小：... 字节
- SHA-256：`...`
```

发版命令：`bash release.sh x.y.z "这里写一句更新说明"`（改版本号 → 打包 → 放进 `apk/` →
回填 `version.json` → 提交推送 → 打 tag，由 Actions 自动发 Release）。
版本号变了记得先跑 `python3 tools/gen-guard.py .` —— 防护链第 4 环钉死了版本号，不重签会被自己拦下。
