# 更新日志

从 **v1.1.3** 开始记录。每发一个版本，在文件最上面加一节，格式照抄下面这份就行。

版本号规则与更新策略见 [README → 更新机制](README.md#更新机制)：**前两位变 = 强制更新，只有第三位变 = 可选更新。**

---

## 1.1.3 — 2026-09-17

文件名 `githup-1.1.3.apk`，包内版本号 **1.1.3**（versionCode `1001003`），Android 7.0 起可用。
签名与 1.1.2 相同（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），**直接覆盖安装，不用卸载**。

### 新增：翻译本页

浏览器上的 GitHub 能用扩展翻译，App 里的 WebView 装不了扩展 —— 所以把翻译做进了 App 自己。

- 右上角**地球图标是总开关**，默认关闭。打开后当前页翻成中文，关掉即还原原文
- **总开关是全局的**：只要不关，换到哪个页面都接着翻，不用每页重新点
- **滚动驱动**：只翻屏幕上下各 600px 范围内的英文段，滚到哪译到哪，屏幕外不翻
- 自动跳过代码、仓库名、用户名、`@某人`、版本号、文件路径
- 译文缓存在本机（上限 600 条），同样的内容不重复请求；菜单可一键清空
- 还原时不重建页面、不重新请求接口

全部免费、开箱即用，不用注册不用 Key，默认**自动探测**谁最快。设置页可手动指定引擎，
长按翻译图标进菜单可切换引擎 / 清缓存。

| 引擎 | 特点 |
| --- | --- |
| 有道 | 国内直连最稳的免费引擎，无需 Key |
| 设备端翻译 | 系统内置，**离线、不联网、文本不出设备**（需装中英文语言包） |
| DeepL | 质量最佳，公共 IP 偶尔限流 |
| Google | 老牌免费接口，需海外网络 |
| MyMemory | 兜底，全球可达，匿名有日配额 |
| ~~微软 Edge~~ | **已停用**：微软关闭了公开令牌接口，实现保留但已移出探测链 |

引擎挂了自动换下一家；某几段没翻出来会隔 4 秒自动重翻，最多 5 轮。

### 新增：平板 / 折叠屏适配

- 不再锁死竖屏：平板横持、分屏、折叠屏展开都跟随系统旋转
- 转屏**不重建 Activity** —— 登录态、翻译开关、滚动位置都不丢
- 宽屏下内容限宽居中（900px），底部标签栏不再被拉成 1280px 平分
- 横屏时左右避开刘海 / 挖孔

### 修复

1. **打开翻译后页面加载变慢**（骨架屏一直转）
   翻译把网络线程池占满了：页面数据和翻译请求共用 4 个线程的池，自动探测时连不上的引擎
   要挂满 20 秒连接超时，而且失败没有记忆、每换一页重来一遍。
   现在：线程池 4 → 8，连接超时 20s → 12s，探测失败进 10 分钟黑名单 + 全败后 2 分钟冷却。
2. **翻译请求在真机上一次都没走原生通道**
   链路是两层（`translate.js` → `window.Native.http`（api.js，收对象）→ `NativeBridge.http`（Java，收字符串）），
   之前多 stringify 了一层，Java 侧 `new JSONObject` 抛异常 → 请求 `status 0` 失败 → 只能退回 fetch 兜底。
   表现就是「只有 MyMemory 和有道能用、Google / DeepL 全灭」，而且每个请求都要失败一次再重试。
   现在只转换一层，原生通道恢复正常。
3. **翻译拖慢、翻一半就停**（1.1.3 之前的遗留问题一并修掉）
   有道接口有频率限制，原来每段一个请求必然撞 `411`；现在换行拼批（≤8 行 / 800 字符）、
   相邻请求间隔 ≥800ms，失败时对半拆分重试而不是退成逐条。
4. **搜索结果只显示第一页，翻不下去**（议题 #1）
   搜索只请求了第 1 页 `per_page=30`，既没传 `page` 也没有翻页入口，于是不管有多少结果都只有那一屏。
   现在底部有「加载更多」，表头显示「共 N 条结果（已加载 M 条）」；
   GitHub 搜索本身最多给 1000 条，到顶会明确提示，不再让人以为软件坏了。
5. **搜索结果点进详情再返回，会重新加载并回到顶部**（议题 #2）
   路由渲染时无条件 `innerHTML=''` + `scrollTop=0`，回退也照做。
   现在按路由记住滚动位置，返回时原样还原；搜索结果按「类型 + 关键词」缓存，返回不再重新请求接口。
6. **issue / README 里的视频和图片看不了**
   视频：Markdown 渲染器把所有原始 HTML 一律丢弃，而 GitHub 上传的视频就是裸的 `<video>` 标签 —— 连影子都没有。
   现在放行一小撮安全的排版/媒体标签（仍有 DOMPurify 兜底），裸的 `.mp4/.mov/.webm/.m4v` 链接也会就地变成播放器，
   并补上 `controls`（GitHub 贴进来的标签未必自带，缺了就是一块按不动的黑砖）；全屏播放和返回键退出全屏也接上了。
   图片：外链图片被包成 `<a target="_blank">` 且不带 `data-zoom`，而 WebView 没开多窗口 —— 点下去既不放大也不跳转。
   现在内外链图片一律可点开，走内置查看器；加载失败的给一个可见的虚线框，不再只剩一片空白。
7. **议题评论里上传的视频 / 截图点开还是看不了**（1.1.3 首发后的补充修复）
   拖拽上传到 GitHub 的附件贴出来就是一行 `…/user-attachments/assets/<uuid>`，**没有扩展名**，
   而上一版的识别靠 `.mp4 / .mov` 后缀，这类链接直接漏掉。更根本的是：
   Markdown 渲染器会把正文里 7~40 位的十六进制串自动链接成 commit，而附件 UUID 恰好全是
   十六进制字符 —— URL 在识别之前就被撕成了「半个链接 + 一个假 commit」。
   现在：SHA 前面是 URL 成分（`/ - . =` 等）的一律不碰；无扩展名附件乐观渲染成播放器
   （只拉 metadata），加载失败自动降级 **视频 → 图片（可点开）→「📎 打开附件」链接**。
   裸的图片链接（`…/xxx.png`）现在也就地显示、可点开。

### 变更：更新规则（前两位变 = 强制）

版本号按 `x.y.z` 三段走，从高位往下比，「第一个出现差异的那一位」决定这次更新是强制还是可选：

| 变化 | 例子 | 行为 |
| --- | --- | --- |
| 第一位（大版本） | `1.1.1` → `2.0.0` | **强制更新**，弹层不可关闭，装好才能继续用 |
| 第二位（功能更新） | `1.1.1` → `1.2.0` | **强制更新**，同上 —— 这条是本次改的，以前是可更可不更 |
| 第三位（修复更新） | `1.1.1` → `1.1.2` | **可选更新**，给「立即更新 / 稍后提醒 / 跳过此版」 |
| 版本号没变、包换了 | `1.1.1` → `1.1.1` | **可选更新**，按安装包指纹（SHA-256）识别，标题写「有新内容可用」 |

**一句话：想让老用户必须升级就把第二位 +1，只是修 bug 就动第三位。**

强制更新时不认「跳过此版」—— 就算用户之前跳过过这个版本号，前两位一变照样拦。

### 已知问题

- **国内手机上通常只有有道和 MyMemory 能用**。Google / DeepL 需要海外网络，这是网络环境决定的，
  不是软件的毛病；微软 Edge 是厂商关停（公开令牌接口已返回 404）。
- 设备端翻译要系统装了中英文语言包才可用，没有就自动跳过。
- 翻译会把**被翻译的文本**发给所选服务商。私有仓库内容介意的话，把引擎固定到「设备端翻译」。

### 校验

- 大小：714,777 字节
- SHA-256：`1b1825f653a658b79c67ff7b5d973bf270c07e11ddae0cbeac7768ad9d6e6c58`

```bash
apksigner verify --print-certs githup-1.1.3.apk
sha256sum githup-1.1.3.apk
```

---

## 以后新增一个版本时

复制下面这段放到 `## 1.1.3` 那一节的**上面**，改掉版本号、日期和条目即可：

```markdown
## x.y.z — 日期

文件名 `githup-x.y.z.apk`，包内版本号 **x.y.z**（versionCode `...`）。

### 新增
- ……

### 修复
- ……

### 已知问题
- ……

### 校验
- 大小：... 字节
- SHA-256：`...`
```

发版命令：`bash release.sh x.y.z "这里写一句更新说明"`（改版本号 → 打包 → 放进 `apk/` →
回填 `version.json` → 提交推送 → 打 tag，由 Actions 自动发 Release）。
版本号变了记得先跑 `python3 tools/gen-guard.py .` —— 防护链第 4 环钉死了版本号，不重签会被自己拦下。
