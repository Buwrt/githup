## githup v1.1.5（朴素样式底栏居中 / 页签对齐官网 / 新手指导可重置 / 设置项改名「iOS风格」/ 顶栏标题靠左 / 搜索按钮放大 / 全机型适配）

> 同一个对外版本号 1.1.5（versionCode `1001005`）下的**第五个包**，可**直接覆盖安装**，不用卸载。
> 本轮修的四处：底栏的字不在版心正中、iOS 底栏悬空离屏底 14px、
> 仓库页的页签样式跟官网不一致、页面内容被顶栏压住。

文件名 `githup-1.1.5.apk`，对外版本号 **1.1.5**（versionCode `1001005`），应用名 **githup**，
包名 `com.hubmobile.app`，Android 7.0 起可用。
签名与本库以往各版本 **完全相同**（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），
可直接覆盖安装，不用卸载。

### 修：朴素样式下底栏的字不在版心正中

关掉 iOS 风格后，底部「首页 / 通知 / 探索 / 搜索 / 我的」的图标和文字整体偏上，
选中项那枚浅灰底衬还压着图标。魅族 20 Pro 上一眼就不对，1+ Ace 上看着正常 ——
查下来是**两个 bug 叠在一起**：

| # | 根因 | 修法 |
|---|---|---|
| 1 | 指示器 `.tab-ind` 的四条边写死 4px，那是给玻璃胶囊（本身有 4px 内衬）配的；朴素样式没有内衬，4px 就没有参照物，灰斑自然套不住字形 | 抽出 `--nav-ind-top / bottom / left / width / height`，两种风格各写一套 |
| 2 | **真正让文字偏上的那条**：底栏是 `align-items: stretch`，每个页签的高度被拉满整条底栏，所以写在底栏上的上下 `padding` 对页签的版心**完全无效** | 内衬改写到页签自己身上（`--tabbar-pad-y`） |

第 2 条也解释了「为什么只有魅族明显」—— 它不是某些机型不对，而是**一直在错**，
只是玻璃模式里两次误差互相抵消了一部分，差得少看不出来；朴素样式把内衬归零，
两个错误才一起露出来。顺带把朴素样式的选中项换成**安卓绿 `#3ddc84`**。

实测（字形组的中心 vs 底栏几何中心，五档机型 × 两种风格）：

```
朴素  393×852 / 360×780 / 320×569 / 480×1040 / 852×393 横屏
      字形中心 31.5/31.5/31.5/31.5/25.5   条中心 32/32/32/32/26   偏差 0.5px
      指示器中心偏差 0.0px（五档全部），上下留白 17/17px，横屏 11/11px
iOS   同上五档：字形偏差 0.5px、指示器偏差 0.0px、上下留白 4/4px，回归无变化
```

### 改：仓库页的页签改成 GitHub 官网那样

| 项 | 改前 | 改后 |
|---|---|---|
| 图标 | 每个页签都挂一个 SVG | **全部去掉**（官网只有文字，`< > 代码` 那对尖括号本身就是字符画的） |
| 高度 | 靠上下 padding 撑出来，带图标 49px、不带 39px，会随图标变 | 固定值：普通 48px、iOS 风格 49px |
| 文字位置 | 偏上 1.5px | 0（正中） |

文字偏上那 1.5px 是个挺隐蔽的坑：中文字体的正常行高是 1.4~1.5，一半给上半、
一半留给「下伸部」，而中日韩字形**根本没有下伸部**，那半行空白就白挂了。
再叠上 `box-sizing: border-box` 被两层边框吃掉 3px（外层 1px 分隔线 + 按钮 2px 选中线），
内容实际只能按 45px 居中、视觉上却是 48px 的条。三处一起改：`line-height: 1`、
选中线换成不占盒模型的 `inset` 阴影、外层改用 `content-box`。

### 修：朴素样式下页面内容被顶栏压住

那段「让开顶栏」的顶部留白原来只写在 iOS 风格的选择器里，等于**关掉 iOS 风格这段留白就消失了**，
第一行内容直接顶到顶栏底下。顶栏在两种风格下都是固定定位，留白不该分风格，已提到基础规则里。
十档机型复测，两种风格现在都是 52px。

---

### 修：iOS 风格的毛玻璃底栏是悬空的

对比图上很显眼：iOS 风格下底栏底边停在 838、屏幕高 852，**底下空着 14px**，
整条玻璃浮在半空 —— 「底栏不在低」有一部分就是它。

那 14px 是胶囊**还透明**的时候为了悬浮感加的（`--nav-lift: var(--tabbar-lift)`）。
现在胶囊有背景、有毛玻璃，抬起来就不叫悬浮了，叫悬空：底下那 14px 会透出别的页面内容。
iOS 真机上底栏和屏幕底边之间那点留白是 **safe-area**（home indicator 区），
不是随手加的常数，而 safe-area 在 `#tabbar` 的 `bottom` 里本来就单独给了一份。

现在归零，两种风格都是 788→852 贴底；变量连同窄屏、横屏两档里的取值一并删掉
（留着就是死变量，下次有人看到 14 还会想加回去），改动处写了注释说明为什么必须是 0。

---

### 新：详细新手指导，十步高亮，随时可跳过

第一次打开自动播放，像游戏新手教学那样 **把该点的地方一个一个圈出来**，
整屏压暗、只留一个洞亮着，旁边配一张说明卡片：

| 步 | 圈哪里 | 说什么 |
|---|---|---|
| 1 | 全屏 | 欢迎使用 githup（不圈任何控件，居中卡片） |
| 2 | 顶栏 | 这里是页面标题栏，标题统一靠左 |
| 3 | 右上角 | 这里的按钮随页面变化，比如搜索、更多 |
| 4 | 底部 | 五个页签在这里切：首页 / 通知 / 探索 / 搜索 / 我的 |
| 5 | 搜索框 | 输入关键字，回车或点按钮开始搜（自动切到搜索页） |
| 6 | 搜索类型 | 仓库 / 用户 / 代码，在这排页签之间换 |
| 7 | 探索时间范围 | 今天 / 本周 / 本月，决定榜单的时间窗口（自动切到探索页） |
| 8 | 探索语言 | 选语言过滤榜单，只关心某一种语言时用（自动切到探索页） |
| 9 | 设置 → iOS风格 | 玻璃风格不喜欢可以在这里关掉（自动切到设置页） |
| 10 | 全屏 | 全部介绍完，开始使用 |

几个不是「做了就行」、而是踩过坑才定下来的细节：

- **每一步都可以跳过**：右上角常驻「跳过」，返回键也能直接退出，不用硬看完。
- **看过了就不再弹**：状态记在本地，只播一次；**设置 → 新手指导** 里可以随时重看，
  那一行还会写「已完成 / 未开始」，点一下从头再播一遍。
- **洞是真的挖在控件上**：按目标元素的实际位置实时计算，目标还没渲染出来就轮询等，
  等不到就退化成居中卡片，而不是画一个空洞。
- **按钮太小、位置太低、屏幕太窄** 这三种情况都会自动换边（洞下方放不下就放上方，
  再不行就居中），并且永远避开右上角的「跳过」按钮。
- 跳过或看完都会把你送回开始引导前的那个页面，不会停在某个陌生页面上。

### 新：新手引导可以在设置里重置

设置里新增 **重置新手引导**：点一下就把引导恢复成「没看过」的状态，
**下次打开 App 会自动重新播放一遍**（不立刻弹，避免和「现在就看」混淆）。
旁边原有的 **新手指导** 一行是「现在立刻再看一遍」，两者分工不同：

| 入口 | 做什么 | 什么时候弹 |
|---|---|---|
| 新手指导 | 清掉记录 + **立刻**从头播一遍 | 立刻 |
| 重置新手引导 | 只清掉记录，**不**立刻播 | 下次打开 App 时自动播 |

重置后设置页的两行会当场刷新（「可重置 → 已是初始状态」、「已完成 → 未开始」），
点完就能确认生效；如果本来就是没看过的状态，会提示「已经是初始状态」而不是空点一下。

### 改：设置里的「液态玻璃底栏」改名为「iOS风格」

开关的行为、默认值（开）、`data-s="glass"` 都没动，只是把名字换成用户实际叫它的那个。
引导第 9 步的文案与高亮目标也跟着同步。

### 改：顶栏标题改回左对齐

首页 / 通知 / 探索 / 搜索 / 我的，**五个页签的顶栏标题统一靠左**。
上一版按 iOS 习惯居中了，但中文标题居中在这套顶栏里读起来是散的，改回左对齐。

### 改：搜索按钮、顶栏按钮都放大了

搜索按钮原来被挤得很小，点起来要瞄。现在输入框和按钮都提到 **40px 最小高度**
（玻璃主题下按钮 44px），顶栏图标按钮 36 → **40px**，
按钮加了 `flex: none` —— 否则旁边输入框一变长就会把它压扁。

### 改：适配市面上所有机型（含魅族这类窄屏和挖孔屏）

| 适配点 | 做法 |
|---|---|
| 左右安全区 | 新增 `--safe-l` / `--safe-r`，横屏和挖孔屏不再被切边；由原生层注入真实内边距 |
| 高度基准 | `100dvh` 优先，`100vh` 兜底，老 WebView 也不会出现底部空白 |
| 窄屏 ≤360px | 底栏间距与抬升量单独收敛 |
| 大屏 400~719px | 单独一档间距 |
| 横屏（高 ≤520px） | 顶栏压到 44px、底栏 52px，别把内容挤没 |
| 系统字体缩放 | 锁 `text-size-adjust: 100%`，防止用户放大字体后布局炸开 |
| 老 WebView 没有玻璃 | 检测到不支持 `backdrop-filter` 就自动降级成近实色，不会出现半透明的脏斑 |

### 此前同一个 1.1.5 里已经改好的

- **悬浮液态玻璃胶囊底栏**：按样板逐像素量出「外圈压暗、顶部提亮」的三段结构重建，
  不再是描一圈白边。
- **设置里可以关掉玻璃**：`设置 → iOS风格`（原「液态玻璃底栏」），默认打开，关掉回到贴底的朴素样式。
- **下载加速通道 3 条 → 5 条**，按实测速度重排（原来按知名度排，实测恰好反着）。
- **下到残缺文件不再报「下载完成」**：发车前先问大小，完成时比对，对不上换道重下。
- **点了更新却没装成、之后不再提醒**：加上安装回执 + 30 分钟有效期，旧包写的记录一律按过期处理。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**
（对源码文件算整体 SHA-256 并内嵌，被改过就报出来）。
本次源码指纹（`SRC_SHA256`）：

```
f93b692bbb85851d9bf513ba574b9dff855d4d7f173485aa63fb08ed221e6cdd
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.1.5.apk` |
| 大小 | 817,420 字节（约 798 KB） |
| SHA-256 | `34c6b0bf21eb1d388415fdfce3f753bb5ff6766ced505376bebc2a357542582e` |
| versionName / versionCode | `1.1.5` / `1001005` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |
