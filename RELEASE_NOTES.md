# githup v1.2.9（在仓库页按返回，一律先回到「代码」）

这一版修的是一个真实现象：**在仓库页的议题 / 拉取请求 / Actions / 发布 / 更多，
以及「更多」里进去的提交记录、分支、标签……上按返回，会去哪儿全看你是怎么进来的。**

## 一、以前：返回键把走过的 tab 倒着走一遍

从「代码」连点「议题 → 拉取请求 → 发布」，想退出去得按三次返回：
先退到「拉取请求」，再退到「议题」，最后才回到「代码」。用户想要的其实是
「退出这一个 tab」，不是把刚才走过的路倒着回放一遍。

更难受的是另一条路：从搜索结果或者通知里直接点进某个仓库的「议题」——
历史里压根没有「代码」这一条，一按返回整个仓库页就没了。明明人还在仓库里，
却被弹回了搜索页。

根因就一句话：**返回键走的是 history 逐级回退，而 tab 之间每次切换都往历史里堆一条**。

| 来路 | 以前按返回 |
|---|---|
| 代码 → 议题 → 拉取请求 → 发布 | 退到「拉取请求」→「议题」→ 三次才回到代码 |
| 搜索结果 → 议题 | 历史里没有「代码」，直接退出仓库页 |
| 通知 → 发布 | 同上 |

## 二、现在：一次返回到代码，再一次返回出仓库

两处一起改：

- **`handleBack()` 新增一步** —— 只要人在这个仓库、且不在代码 tab，返回就落到代码 tab；
  已经在代码 tab 了，才继续往上退（回搜索、回首页……）。这里用 `replace` 而不是 `go`：
  再 push 一条的话，历史会攒成「代码 → 议题 → 代码 → 议题」，按返回变成在这两页之间来回跳，
  永远退不出去。
- **`pushRoute()` 把「同一个仓库里换 tab」认定为横向切换**，跟底部五个标签一样不堆历史。
  那一排 tab 是仓库页内部的并列视图，来回切不该留下痕迹。

于是历史栈里始终只有「进仓库前那一页 + 当前 tab」两条：
**一次返回到代码，再一次返回出仓库。**

## 三、该逐级退的地方，一步都不能跳

新规则只管 tab 层，下面是刻意不动的：

| 位置 | 按返回 |
|---|---|
| `tree` / `blob` 文件浏览 | 它们本来就在代码 tab 里，照旧逐级退回仓库根 |
| 议题详情 / PR 详情 / 运行详情 / 提交详情 / Release 详情 | 先退回它自己那个 tab |
| 搜索 / 通知 / 个人页 / 下载管理 | 与仓库无关，不受影响 |

判据是「这个路径是不是某个仓库的 tab 层」—— `/owner/repo` 后面最多只跟一个 tab 名，
带文件路径、议题号、commit sha、tag 的都不算。

## 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**
（对源码文件算整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
d9109862572d480760b76a2a7b11662c3d71a14a8ded2ddbccf6720a0e7e7787
```

## 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.9.apk` |
| 大小 | 920,978 字节（约 899 KB） |
| SHA-256 | `8d3ad5df65fc1e8582551818d1e329f026b0ebf00e43b2f94c0ea54d513daef7` |
| versionName / versionCode | `1.2.9` / `1002009` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

> 更新时才提醒，已是最新版完全静默。签名证书与之前一致，可直接覆盖安装，不用卸载。

---

# githup v1.2.8（点「简体中文」看到的是中文正文，「关于」并进页首，图标不再裂，tab 栏不再回弹）

这一版修的是五个真实现象。

### 一、点一下「简体中文」，看到的是中文正文，不是一屏行号

以前点那一行语言切换有两个毛病：先是**像重启一样跳回首页**（下面第二节），
修完之后落点仍然不对 —— 跳过去看到的是**满屏行号**：README.zh-CN.md 的源码，
一行行 `<a href="README.zh-CN.md">` 这样的原始文本。

官网在文件页对 `.md` 默认给的就是 **Preview**（排版好的正文），源码要自己切过去。
现在 App 也是双态的：

- README 里的语言切换、目录里点开一份 `.md` → **渲染**（官网 Preview 同款）；
- README 卡片右上角那枚「查看源码」→ **源码**，按钮写的是源码就落在源码上；
- 切换条在两种状态下都长在，从源码进来也能一键切回正文。

顺带：正文不贴边（`.md` 不带外边距，文件页自己补）；相对图片按这份文档自己的目录去补，
`docs/guide.md` 里的 `./cover.png` 补成 `…/main/docs/cover.png`；
渲染态下点「复制」不再复制出一份空内容。

### 二、仓库顶上那条摘要和「关于」卡写着同一批信息

描述写了两遍、star / fork / 关注上下各一排，中间还隔着整屏文件列表。现在「关于」卡的正文
（描述 / topics / 主页链接 / 许可证 / 默认分支 / 更新时间 / 统计）整体上移进页首摘要条 ——
信息还是那些，只留一处：描述 → 可点的 topics 标签 → 主页链接 → 统计行（star / fork / 关注 /
语言 / 许可证 / 分支 / 更新于）。「关于」卡本身不再单独存在，只留下跟正文无关的克隆地址块，
独立成一张卡。只在代码 tab 这么排：议题 / PR / Actions 顶一坨标签没意义，保持原样。

### 三、点一下链接，软件像重启了一样

在仓库页的 README 里点那一行语言切换（`English · 简体中文 · 日本語`），以前会白一下、
然后回到首页 —— 看着像崩溃，其实是 **WebView 去加载了一个不存在的本地文件**：
README 里那几个 `<a href="README.zh-CN.md">` 是原生 HTML，不经过链接渲染器，
相对地址一直原样留在 DOM 里，被 WebView 拿页面地址（`file:///android_asset/web/index.html`）
解析成了 `file:///android_asset/web/README.zh-CN.md`。本地没有这个文件；
而 App 里恰好又有「本地资源加载失败就重载首页」这条兜底，一次小失误就被放大成整页重启。

三处一起改：

- **源头**：渲染时把容器里所有 `<a>` 的 href 统一规整 —— 相对链接补成
  `#/{仓库}/blob/{分支}/{目录补全后的路径}`，GitHub 链接交给链接映射换成站内路由，
  App 里确实没有的（讨论区、Wiki）保持外链；
- **闸**：`file://` 导航只放行 SPA 入口，其余吃掉，不再让 WebView 去撞不存在的文件；
- **兜底**：只有入口本身加载失败才重载首页，一张裂图、一个漏网链接不再掀翻全局。

顺带把点击分流理顺了：站内路由、页内锚点、外链（内置浏览器）、
Release 资产（原生下载通道）各走各的，不再出现「点锚点跳到一条莫名其妙的页面」。

### 四、图标永远裂着，同一页的截图却好好的

PNG 出得来、SVG 出不来，差别只在格式上。把可能导致 SVG 渲染不出来的口子依次堵上：

- **MIME 说不清格式**：有的 CDN 把 SVG 回成 `text/plain`、缓存丢了侧标会落成
  `octet-stream` —— 这两种类型 WebView 一律拒绝当成图片渲染，字节是完好的图也没用。
  现在交出字节前按扩展名最后把关一次，网络与缓存两条来源都覆盖；
- **兜底队列只认「监听之后」的失败**：几 KB 的小图常常在监听绑上之前就失败完了，
  于是永远没有第二次机会（大图反而因为下载慢总能赶上）——这类一并送进兜底队列；
- **兜底认不出 SVG**：补上 SVG 的文本前缀识别，响应头丢了也能认出来。

还顺手修了一处：相对链接以前一律钉死在默认分支（`blob/HEAD`），
在 `release/v1.2` 这种分支上看 README 会指错地方；现在跟着当前分支走。

### 五、tab 栏「滚过去又弹回来」

上一版修的是「点了哪个 tab 被提前忘掉」，改完少了很多，但没绝根 —— 剩下的那部分
跟点了谁无关，是 CSS 里一句看着纯装饰的属性在捣乱：

```css
.tabs { … scroll-behavior: smooth; }
```

加它本想让这一排滑得顺一点。可 **`scroll-behavior` 只管脚本调用 / 导航触发的滚动，
手指拖动和惯性滑动属于用户输入，它管不着** —— 对手感零收益，却偏偏管得着我们要用的
那条路：设置 `scrollLeft` 从「一步到位」变成了几百毫秒的动画；动画没跑完，接口回来
触发的第二次渲染就把这一排按钮整块重建，新元素从 0 重新开始 —— 于是「滚过去一半又
弹回最左」。**接口快时必现，慢时看不出来**，正好就是「有时候」。

附带还咬一口：动画期间 scroll 事件连着派发，中间值被当成「用户滑到这儿」记进了位置
记忆，下次进来停在谁都没选过的半路上。

现在把这句去掉（显式写 `auto`），程序设置一律走 instant 的新 `setTabScroll()`；
并且自己刚设的那一下不再被当成用户操作 —— 既不写进记忆，也不清掉「用户点了哪个 tab」。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**（对源码文件算
整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
52a6da01f6ba6d72462729acd8c051e0a88ecf5a5913b26dd6d7bfe519b9d202
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.8.apk` |
| 大小 | 919,670 字节（约 898 KB） |
| SHA-256 | `4a4ef675a74eb6bcc95bde4050ff80c9f0f297f146040642fda31fa8f467f2cd` |
| versionName / versionCode | `1.2.8` / `1002008` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

> 更新时才提醒，已是最新版完全静默。签名证书与之前一致，可直接覆盖安装，不用卸载。

---

## githup v1.2.7（搜索框里贴一条链接，直接落到 App 里那一页）

从别处复制来一条 `https://github.com/xxx/yyy`，以前唯一的去处是把它当关键词在
搜索框里搜一遍、再从结果里挑一遍，真正的那一页还得回浏览器去开。现在**搜索框
认得 GitHub 链接**：贴进去，输入框下面立刻告诉你会去哪一页，回车或点一下，
就落在 App 里对应的原生页面上。

### 一、认哪些

| 你贴的是 | 去哪 |
|---|---|
| `github.com/owner` | 用户 / 组织主页 |
| `github.com/owner/repo` | 仓库首页 |
| `.../tree/<分支>/<路径>`、`.../blob/<分支>/<文件>` | 那个分支上的目录 / 文件（`blame` 也算，落在同一个文件上） |
| `.../issues/123`、`.../pull/123`（后面带 `/files`、`/commits` 同样认） | 议题 / 拉取请求详情 |
| `.../releases/tag/v13.6.0`、`.../releases/latest` | 对应版本的发布详情 |
| `.../commit/<sha>`、`.../commits/<分支>`、`.../actions/runs/<id>` | 提交 / 提交历史 / Actions 运行记录 |
| `.../pulls`、`.../releases`、`.../stargazers`、`.../milestones`… | 仓库里对应的那一页 |
| `gist.github.com/<id>` | Gist 详情（带不带用户名都能认） |
| `raw.githubusercontent.com/owner/repo/<分支>/<文件>` | 等价于那个分支上的这一份文件 |
| `.../releases/download/<tag>/xxx.apk`、`codeload.github.com/…zip` | **下载**，交给 App 自己的下载管理（带认证头） |
| `github.com/notifications`、`/explore`、`/issues`、`/pulls`、`/gists` | App 里同义的那一页 |
| `github.com/topics/<话题>` | 等价于 `topic:<话题>` 的搜索，落到结果列表 |

`https://` 和 `www.` 可以不打，`?utm_source=…`、`#issuecomment-xxx` 这类尾巴一律丢掉。

### 二、够不着的，明说而不是硬塞

**认不出来就停在搜索上**，这是第一条规矩 —— 猜错比不跳糟糕得多。
搜索词里夹一个链接（`README 里写了 github.com/x`）不等于你想打开它，所以只对
「整串就是一条链接」买单。

是 GitHub 链接、但 App 里确实没有那一页的（网页端的设置、marketplace、wiki、
discussions、compare 这些），提示条会写明白**「用内置浏览器打开」**。硬塞进路由
的话，`parseHash` 会把它的第 3 段当成文件路径，最后落到一个空空的代码页上 ——
那还不如直接交给浏览器。

下载地址（Release 里的安装包、codeload 的源码打包）进原生下载通道，而不是
「打开一个二进制文件页面」：浏览器的下载管理器受分区存储限制，下完还得去浏览器
里翻；发布详情页那份 APK 走的就是原生通道，行为必须一致。

### 三、粘贴完不自动跳

手一松就被甩到另一个页面上太吓人。提示条出来之后，走不走由你决定 —— 点提示条、
回车、或者点「搜索」都行。原来那个「停下来自动搜」的防抖在遇到链接时会直接
**跳过**：宁可等一个明确动作。

提示条下面还有一枚**仍然搜索**：万一你真的就想拿这串 URL 去搜，路没被堵死。

### 四、顺带修掉的两处暗伤

- **tag 与 ref 改走查询参数。** 标签名自带斜杠的仓库不止一家（`release/2026-09`），
  放在路径第 4 段会被 `split('/')` 切碎，只剩第一段去查 —— 必然 404。
  现在 `/owner/repo/releases?tag=…` 和 `/owner/repo/blob/<路径>?ref=…` 都走查询参数，
  带几个斜杠都传得过去；老形式继续认，历史链接不失效。
- **`releases/latest` 不再是「查一个叫 latest 的标签」。** 换成另一条端点，
  以前这一跳是必然 404。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**（对源码文件算
整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
8c127467c9e17d64bb13e27b3c4aad80acc9241219e6e0ffe94d725869c827f5
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.7.apk` |
| 大小 | 913,402 字节（约 892 KB） |
| SHA-256 | `c7843c3d170ff1be15e5c1321589c1722c52fb59002f7badf6a12e3b09eaa399` |
| versionName / versionCode | `1.2.7` / `1002007` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

> 更新时才提醒，已是最新版完全静默。签名证书与之前一致，可直接覆盖安装，不用卸载。

---

## githup v1.2.6（仓库页 tab 栏三合一 / tab 位置不再跑 / README 图片把快车道修回来）

这一版动的全是手感上的毛病：**仓库页那一排 tab**（代码 / 议题 / 拉取请求 / Actions /
发布 / 更多）拖动时的卡顿与误刷新、**每次刷新位置就跑回去**、以及 **README 里的图片**
——先解决「大图根本出不来」，再解决「出来得太慢」。

上一版 v1.2.5（Star 列表分页、撤销 / 回滚提交）的记录见下面那一节。

### 一、那一排 tab：卡顿、回弹、误刷新，其实是同一个病

三个症状看着互不相干，动手之后发现都出自同一处：下拉刷新**只看手指走了多远，
不看往哪走**。

```js
// 改之前：只算 dy，横向滑动必然带着几像素竖向抖动，攒够 70px 就整页刷新
if (dy > 70 && view.scrollTop <= 0) { pulling = false; UI.haptic(); Router.reload(); }
```

在 tab 栏上横着拖，手指多半会斜个几像素，于是：

| 症状 | 真凶 | 现在 |
|---|---|---|
| 在 tab 栏上横拖，页面被带着下拉、松手整页重载 | `touchmove` 不做方向判定 | 先判方向：**横向为主就不算下拉**；再给 8px 死区，微抖动不算数 |
| 滚到最左 / 最右端再接着拖，整页被拽着动（scroll chaining） | `.tabs` 缺 `overscroll-behavior-x: contain` | 补 `contain`，把滚动锁在这一层里 |
| 手指抬起之后不跟手、一卡一卡 | 缺 `-webkit-overflow-scrolling: touch` | 补上，惯性滚动交给合成器线程，不再掉帧 |

**特意没有加** `touch-action: pan-x`：那会禁止在这一栏上竖向滚页面，手指落在这一条
44px 高的地方想上滑就会失灵 —— 等于治好一个毛病换个新的。

### 二、tab 位置：刷一下页面就跑回最左

以前每次渲染都是全新 DOM，`scrollLeft` 一律归零：**为了够到最右的「发布」「更多」，
每次都得重新拉一遍，拉完一刷新又弹回去。**

现在按仓库各自记住位置：

- **SPA 内部切 tab** —— 内存里的即时值，够快；
- **App 重启 / WebView 重载** —— JS 重新执行，内存没了，回落到落在本地存储里那份；
- **区分「主动导航」和「被动刷新」**：你亲手点了某个 tab，就把它滚进视野（点哪个该看见哪个）；
  单纯刷新则**一步都不挪** —— 这正是「刷新一下又跑了」的那一半，
  不能因为高亮项（比如最左的「代码」）不在视野里就把它拉回去。

### 三、README 的图片：先让它出来，再让它快

**第一轮 —— 大图不是慢，是根本回不来**

图片不走 WebView 直连（raw 域在不少网络下不通），而是原生拉回 base64 交给前端拼成
data URI。最后一跳 `evaluateJavascript` 底层是 **Binder IPC，单次事务上限约 1MB**：

| | |
|---|---|
| 这张赞赏码 | 984×1398 · **417 KB** |
| base64 之后 | **569,260 字符（556 KB）** |
| 再叠上并发 | 一次要过 **2 MB 以上** —— 一张都回不来 |

所以：**小图没事，只有大图挂**。修法是两件事：

- **分片下发**：超过 48 KB 的响应不再一次性过 IPC，改成 `_begin` 登记份数、`_chunk` 逐片塞，
  前端拼齐再交付（`JsBridge.emitB64` + `api.js` 的 `_begin/_chunk`）。48 KB 的取值让
  普通 API 响应压根不会走这条路，只有真正的图片/大附件才会。
- **别重复拉同一张图**：`md.js` 里带标题的链接（表里那种 `[`路径`](xxx.png)`）也被渲染成了 `<img>`，
  官网渲染成链接，App 却渲染成图 —— 同一个 README 出现 5 张图、发出 5 次请求。
  现在与官网一致渲染成链接，并给同一 URL 加了「已完成 + 在路上」两级去重。

同一份 README：**`<img>` 从 5 张回到 3 张，网络请求从 5 次降到 1 次。**

**第二轮 —— 能出来，但比官网慢得多**

对照官网抓回来的响应头，答案是**它什么都没优化，它只是没挡在浏览器的路上**：

| | 官网 | App（第一轮之后） |
|---|---|---|
| 传那张 417 KB 的图 | 417 KB | **569 KB**（base64 后 +33%） |
| 来回过 Binder | 不用 | **13 次** |
| 编码 / 解码 | 解码 1 次 | 各多一次（还要先 Unicode 化） |
| HTTP 缓存 | 有（CDN 边缘 `X-Cache: HIT` + 浏览器缓存） | **完全没有** |
| 渐进显示 | 边下边显 | 必须整张到齐 |

这一版新增 `ImageProxy`：在 WebView **伸手取图的那一刻**把字节接住
（系统给的口子 `shouldInterceptRequest`），用已经跑通的原生网络栈去拿，
**原样字节**直接交还给 WebView 自己解码 —— 不经 String、不经 Binder、不经 JS。
浏览器天生那几样本事也就跟着回来了。在这之上再补三件事：

| 做法 | 效果 |
|---|---|
| 落到本地的图片缓存 | 第二次打开同一张图 = 读本地文件，**零网络** |
| 渲染完先把前 8 张悄悄拉下来 | 懒加载是滑到眼前才发请求；提前灌进去，滑到时已经是本地文件 |
| 匿名优先、撞墙才补令牌 | 带 `Authorization` 的请求共享缓存一律不收 —— 公开图匿名拉才能吃到 CDN 那一记 `X-Cache: HIT`；私有附件拉到 404 时再自动带着令牌重试 |

**失败永远不该变得更糟**：这一层任何一步拿不准，一律返回 `null` 让 WebView 照原路
自己取；真遇上取不到的情况，前端还会再退回老的 base64 通道试一次 —— 那条路自带令牌。
最坏也就是现在这个样子，不会多出一张裂图。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**（对源码文件算
整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
4a94c9c2ef7a8e9d57bbd5924bf44e5f4407ab23cbd38fa3fe1b8e4bb1ea81aa
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.6.apk` |
| 大小 | 905,045 字节（约 884 KB） |
| SHA-256 | `89cbfa0d321ad51b7700c51beea458f4850931c583802e0b5cb2616d3a157210` |
| versionName / versionCode | `1.2.6` / `1002006` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

> 更新时才提醒，已是最新版完全静默。签名证书与之前一致，可直接覆盖安装，不用卸载。

---

## githup v1.2.5（Star 加载不全的根因 / 撤销与回滚提交）

这一版主要处理两件一直挂着的事：Star 列表**超过 100 个就加载不全**，以及
提交记录上缺的**撤销 / 回滚**。主干改动来自 [@mymine](https://github.com/mymine)
提的 [PR #7](https://github.com/Buwrt/githup/pull/7)，合并时又补了三处结实性问题。

### 一、Star 列表加载不全

列表从来只打一次接口（`per_page=100`、不翻页），所以 star 一过百，后面的仓库
压根没被请求过 —— 450 个 star 的账号实际只拿到 100 个。现在顺着响应 Link 头里的
`next` 一页页往下取，最多 10 页（1000 条），够覆盖绝大多数账号；而且每拿到一页
就先画一遍，不用盯着空白等全部页回来。

### 二、「最近更新」为什么排错

上一轮修的是排序键和时间字段，但有一种症状解释不了：**首次打开是对的，停几秒就
乱掉，之后重开也乱**。真凶在取数参数 —— `/starred` 这个接口**只认 `created` 和
`updated`**，而代码照着 `/repos` 那套写了 `sort=pushed`。服务端对不认识的参数
**静默忽略、不报错**，于是这一档拿回的一直是默认的 `created` 顺序，在这批数据里
再怎么重排，也排不出真正的「最近更新」。

### 三、提交记录上多了两枚按钮

Commits 列表每行行尾多了一枚 ⋮，点进去是两项容易混淆、后果却完全不同的操作：

| 操作 | 相当于 | 会丢历史吗 |
|---|---|---|
| **撤销提交** | `git revert <sha>` | 不会 —— 在分支顶端**新增一个反向提交**抵消掉改动，原来的提交还留在历史里，可随时再看 |
| **回滚提交** | `git reset --hard <sha>` | **会** —— 分支指针直接挪回去，它之后的提交从这条分支上消失 |

撤销是 GitHub REST 里没有现成接口的（GraphQL 那个只对已合并的 PR 有效），所以
用 Git Data API 手工拼的：读目标提交 → 读父提交的整棵树 → **只搬 blob 的 sha**
（二进制文件同样成立，不必下载内容）→ 以当前分支顶端为 `base_tree` 建新树 →
建提交 → 更新 ref。这样即便被撤销的提交之后又有新提交，反向提交也能正确叠在最上面。

根提交、合并提交、没有文件改动的提交都会被明确挡下来，提示是「到网页端处理」而不是
给一句看不懂的报错。

### 四、合并时补上的三处

1. **文件 mode 不再写死 `100644`。** 原实现一律按普通文件恢复，于是可执行文件
   （`100755`）丢了可执行位，符号链接（`120000`）被还原成「内容是目标路径的一坨文本」。
   改成从父提交那棵树里把原始 mode 一起带出来。
2. **单个提交最多只能拿回 300 个改动文件**，而且接口不像 `/compare` 那样给
   `truncated` 标记 —— 超限时文件列表会被悄悄砍掉一截，反向提交就**少还原一部分
   还不报错**。现在到上限直接拒做并提示去网页端：宁可被拒，也不能静默交出一个半成品。
3. **回滚的门槛抬高。** 它本质是 force push，若「有写权限」就能触发，等于给每个
   协作者发了「抹掉主分支历史」的能力。现在按 Maintain / Admin 判定，不够格时菜单项
   **置灰并写明「需要 Admin / Maintain」**（照惯例如此：置灰而不是藏起来），
   入口函数里再拦一道。

### 五、致谢

感谢 [@mymine](https://github.com/mymine)。他先在 issue #5 里一路追到「450 多个
star 只加载不到 100 个」，随后直接动手把代码改好了 —— `sort=updated` 那处根因、
以及 `__bound` 委托机制的用法都对得很准（那个约定此前全仓库没有页面用过，他是
第一个正确接上的）。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**（对源码文件算
整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
bad58e99f57929e4b594192d8e381ce8d6485544f7108c0ecbdbfeaf70f5a18d
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.5.apk` |
| 大小 | 894,949 字节（约 874 KB） |
| SHA-256 | `c1666b59dcf364471217ed17c531b172fc7c111498cc9a88ff95777ff7526b24` |
| versionName / versionCode | `1.2.5` / `1002005` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

> 更新时才提醒，已是最新版完全静默。签名证书与之前一致，可直接覆盖安装，不用卸载。

---

## githup v1.2.4（协作者与邀请）

以前想知道「谁能动这个仓库」，只能回电脑前面打开网页端 —— App 里压根没有入口。
这一版把这件事搬进来了：**设置页**多了一块「协作者与邀请」，点进去就能邀请人、
改权限、移除，以及处理发了但对方还没接受的邀请。

### 先把五个权限等级说清楚

等级这种东西，光看英文缩写没人知道差在哪，所以每一档都配了一句说明：

| 等级 | 能做什么 |
|---|---|
| **Read（只读）** | 能看、能 clone、能开议题、能评论。给只是想围观的人 |
| **Triage（分类）** | 上面全部，外加能管议题和 PR —— 打标签、指派、关里程碑、标重复。但一行代码都不能写。适合帮你当客服、整理议题的人 |
| **Write（写）** | 上面全部，外加能推分支、能合并 PR。日常协作者默认给这个 |
| **Maintain（维护）** | 上面全部，外加能管仓库大部分设置（分支保护、webhook 这类）。**但**不能删仓库、不能转让、不能改可见性 |
| **Admin（管理员）** | 全部 —— 删仓库、转让、改可见性、管安全设置、再邀请别人 |

选等级的时候这五句会整段列出来，不用先退出去查文档。

### 能做的四件事

**邀请协作者** —— 输用户名 → 选权限等级 → 发出去。对**组织内**的人是点了就立刻生效；
对**组织外**的人是发一封邀请邮件，进到「待处理邀请」那一块，等对方接受才算数。
发完会告诉你属于哪一种，不用自己猜。

**改权限** —— 列表里点某人 → 重新选等级 → 立刻生效，不用再确认一遍。

**移除** —— **长按**某一条出菜单，里面有「移除协作者」。点了会二次确认，
并且把后果说清楚：**他立刻失去这个仓库的访问权，但他已经 fork 出去的仓库不受影响**。
仓库所有者不能被改权限也不能被移除（服务端本来就会拒，与其让人点半天收到一句
「操作失败」，不如一开始就挡掉）。

**待处理邀请** —— 单独一块，列出发了还没被接受的。点进去能**改这条邀请的权限**
（邀请的是 Write，人还没答应，现在想改成 Read 也来得及）、**复制链接**去催他、
**撤销邀请**（撤销后他点开那个链接会失效）。

### 不是 Admin 时：置灰，而不是消失

页面顶部先告诉你**你在这个仓库是第几档**，并把那一档的说明也列出来。
不是 Admin 时，邀请 / 改权限 / 移除这些按钮一律**置灰**，下面写一句
「邀请、改权限、移除都只有 Admin 能做」。

这里刻意选了置灰而不是隐藏：让人看见「有这个操作，只是我现在级别不够」，
比整个按钮消失、让人以为软件没这功能要好懂得多。

### 一个必须单独记一笔的坑

GitHub 对**同一件事用了三套叫法**：

- 协作者列表 / 加人改权限：`pull` / `triage` / `push` / `maintain` / `admin`
- 邀请（列出、改、撤）：`read` / `triage` / `write` / `maintain` / `admin`
- 查某个人的权限：返回的又是 `read` / `write` / `admin`

也就是说「Read」在邀请那边得发成 `read`、在协作者那边得发成 `pull`；
「Write」同理是 `write` / `push`。**写反了不报错**，请求照样 200，
只是等级悄悄对不上 —— 这种错最难查。现在全部集中在一处映射，
并且加了「往返一致」的断言（五档各转过去再转回来必须还是自己）。

### 顺带修掉的一处

路由白名单里漏登记了 `milestones`，所以「更多 → 里程碑」点进去其实会落到代码页
（第 3 段不在白名单里就被当成文件路径了）。这次补上，同时加了注释提醒：
以后每加一个 tab 都得来这里登记。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**（对源码文件算
整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
e38fce4ace2d87d3f8aece3a3e481cdfafe6bb61927581689b969f790e108bf6
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.4.apk` |
| 大小 | 890,205 字节（约 869 KB） |
| SHA-256 | `4d6837519acde8e853f68f6686750554936417cd31327a651c0106bb482ce671` |
| versionName / versionCode | `1.2.4` / `1002004` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

---

## githup v1.2.3（Star 列表「最近更新」停留几秒后排序错乱）

刚登录、点开「我的 Star」的「最近更新」，顺序是好的；在同一个页面停留几秒，
列表突然重排成乱的；此后重新进页面、甚至清空数据重新登录，还是乱的。

根因是**排序用的时间和卡片上显示的时间不是同一个字段**：

- 一进页面，发布时间还没问回来，列表按 `pushed_at` 排 —— 这一步是对的；
- 几秒后，挨个仓库问回来的最新版本时间到了，整表改成按**版本时间**排；
- 可卡片上那串时间写死的是 `repoRow` 里的 `updated_at`，跟排序用的根本不是一个字段。

于是从上往下扫会看到「3 天前、1 周前、2 天前……突然跳到 150 天前」。还有三个放大因素：

- 发过版的整片沉底（版本时间常常是去年、前年），没发过版的整片浮上来
  （退回 `pushed_at`，常常是几天前），列表变成上下两截拼起来的，中间接不上；
- 一次只问前 60 条（`REL_MAX = 60`），而 `/starred` 一页是 100 条 ——
  后 40 条压根没问，跟前 60 条用的不是同一把尺子；
- 发布时间缓存 10 分钟，所以「重新打开依旧错乱」—— 一进来就已经是乱的。

现在改成 **排序键 = 卡片上显示的时间 = max(发布时间, 最近一次推代码)**：

- **必然单调**：显示的那串就是排序用的那串，往下扫只会越来越旧；
- **不再跳变**：还没查到发布时间时，结果就是 `pushed_at`，跟一进页面的顺序
  完全相同 —— 不会再出现「先是好的、几秒后乱掉」；
- **发版依然算数**：只有发版比最近一次推代码还新时才把这一行往上提
  （发完版又补传过安装包的，正是靠这一下提到前面）；
- **角标也不再添乱**：版本时间比最近一次推代码还老时不挂「新版」角标，
  免得一行里同时出现「3 天前」和「新版 2 年前」——那看着又像顺序乱了；
- `REL_MAX` 提到 100，跟每页条数对齐，整页按同一把尺子排。

仿真实测（100 条仓库，直接拿源码里的排序函数跑）：相邻行「上面比下面还旧」的次数，
修复前 **9 → 27**，修复后 **9 → 9 → 0**。

「我的仓库」列表用的是同一处逻辑，一起修好了。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**（对源码文件算
整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
554b431d8c3f97422444eaaa73962add8e6ea5cc7585b5c075cde1d407fee284
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.3.apk` |
| 大小 | 884,765 字节（约 864 KB） |
| SHA-256 | `134bcf95202dbccaca91de102381a30d9605a01434a8127da6c1e03d064388ff` |
| versionName / versionCode | `1.2.3` / `1002003` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

---

## githup v1.2.2（议题里传图失败）

在议题或评论里点「插入图片 / 视频」，挑完照片转两圈，弹出一句「上传失败：Not Found」。

原因是接口打错了地方。以前走的是网页端那套老三步，第一步申请上传策略时打的是
`https://api.github.com/upload/policies/assets` —— 可 `/upload/...` 是 **github.com
（网页）** 的路由，api.github.com 上根本没有这条，服务端直接回 404，响应体里只有一句
`Not Found`。前端把这句原样抛出来，就成了截图上那行字。照片其实一次都没离开手机。

现在换成网页端拖拽上传用的那条直传接口，一步到位：文件名、类型和仓库 id 走 URL 参数，
请求体就是文件本身，成功直接回 `https://github.com/user-attachments/assets/…`。
顺带的好处是附件会**继承仓库的可见性** —— 传在私有仓库里的截图，外人打不开。

老三步没删，留作兜底（直传接口万一哪天被改掉才用得上），里面的域名也改成了正确的
`github.com`。

另外顺手修了几处：

- 大小上限按类型分开：图片 / 普通文件 10MB，视频 100MB。以前一律按 25MB 卡，
  结果不少图在服务端被拒，本地却连句提示都没有。
- 错误开始说人话：401 提示重新登录，403 提示没权限或撞上限流，422 把服务端的原因
  原样带出来 —— 不再对着一句「Not Found」发呆。
- 上传不再把整个文件读进内存（新增流式通道），十来 MB 的原图在低端机上也不会撑着。
- 原生侧万一没回音，2 分钟判失败，按钮不会一直转。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**（对源码文件算
整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
2e8fca79b9448df5ab9d0ac3932237b27f975745acbc8c3f3fc69d0fe11c341d
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.2.apk` |
| 大小 | 883,349 字节（约 863 KB） |
| SHA-256 | `663007df1ff7a9cf4e9d379ad81cbda0d057052989195e71cbf15f72366fd94e` |
| versionName / versionCode | `1.2.2` / `1002002` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

---

## githup v1.2.1（私有仓库看不见 / 「最近更新」排错 / Star 默认档）

三处都是「看得到的信息不对」，一处是漏了参数、一处是取错了字段、一处是默认档不合用。

### 我的仓库列表里看不到私有仓库

自己看自己的列表时，走的是 `/users/:login/repos` —— 那个接口**只返回公开仓库**，
哪怕令牌带着完整的 `repo` 权限，私有的一样拿不到。

改成自己看自己时走 `/user/repos`（公开 + 私有都有），并显式带 `visibility=all`。

### 「最近更新」排出来是「最久没动的排最前」

取数时只给了 `sort=updated`，**没给 `direction`**。GitHub 的 sort 参数默认升序，
于是「最近更新」变成了「从旧到新」，最久没人碰的排最上面。

现在除「按名称」外一律显式 `direction=desc`。

### 「最近更新」排的不是我关心的那种更新

上面修完顺序还是不对 —— `updated_at` 什么都记：改一句描述、被人点个 Star、加个
topic 都算更新。于是列表里飘着一批根本没有新版本的仓库，真正刚发版的反而靠后。

现在这一档改成挨个仓库问最新 Release：

- **发过版** → `max(published_at, 资产里最新的 updated_at)`，发版后又补传 / 换过包的
  以包文件时间为准
- **没发过版** → 退回最后一次推代码的时间（`pushed_at`）

两种混在一起从新到旧排。取数也换成 `sort=pushed`，它跟「发没发新版」强相关。

显示也跟着换了：排序用版本时间、卡片却显示 `updated_at`，屏幕上那串时间就不单调，
看着**还是**像排序坏了。现在这档下卡片显示「新版 x 天前」，没发过版的标「无发布」。

版本号时间缓存 10 分钟，一次最多问 60 个仓库、并发 6；列表先按兜底顺序画出来，
时间补齐后再排一次 —— 不盯着空白等几十个请求。

### Star 列表：默认档改成「最近更新」

三档变成 **最近更新 / 最近 Star / Star 数**，默认落在「最近更新」——来这一页是想看
「我关注的这些哪个出新版本了」。「Star 时间」改名「最近 Star」，方向改成从新到旧
（既然叫「最近」，就是刚刚 Star 的排最前）。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**（对源码文件算
整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
c37c7a7549ce0286db38732aaa40372a7ce9c867c47c9498c8b3be68d5675d0f
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.1.apk` |
| 大小 | 881,345 字节 |
| SHA-256 | `960748fd197616a99afa208318ba28c6ea08031f48ddc401233835dd62253b1f` |
| versionName / versionCode | `1.2.1` / `1002001` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

---

## githup v1.2.0（补七个高频 API 缺口：在线改文件 / 评论可改可删 / 里程碑 / Gist 创建）

这一版要解决的问题是：**很多事在 App 里看得见，却必须回电脑前面才能做。**

动工之前先盘了一遍，七项里**有三项其实早就做好了**——PR 评审闭环、PR 合并、代码搜索，
都有完整实现，只是入口藏得深。真正新加的是四项，另外给代码搜索补了鉴权提示。
（缺的那四项全是「写」：改得动、删得掉、建得出来。）

### 一、在线编辑已有文件

以前想改一行注释，唯一的办法是把文件下载到手机、改完再从本地整个传回去，二进制文件
和超过 1MB 的根本传不动。

现在文件页右上多了一枚**编辑**：正文直接预填进编辑框，改完填一句提交信息就推上去了。
同一套界面还管**新建文件**（目录页面包屑上的「＋」），填路径、写内容、选分支。

`contents` 接口靠**带不带 sha** 区分新建与更新：带 sha 是更新，不带是新建。所以两件事
共用一套代码，只是预填来源不同。两个坑都堵了：

- 那个 sha 是**当前分支**上的。要把改动提交到别的分支时，同一路径指向的 blob 未必是
  同一个，所以换分支会重新去目标分支取一次 sha，而不是复用原来的。
- 超过 1MB 的文件 GitHub 不吐 `content`。这时不假装成功，直说「太大或在当前 ref 上
  不存在」，并留一条「仍然新建」的退路。

### 二、评论可以改、可以删

自己发的评论右上角多了个「⋯」：编辑走 `PATCH /issues/comments/:id`，删除走 `DELETE`
同一个地址。别人的评论不给这个按钮——接口会拒，摆着是骗人。

对着某一行发的那些**行内评论**不在此列：它们走另一套 `/pulls/comments` 接口，时间线
里根本拿不到它们的 id。

### 三、里程碑管理

以前 App 只会在议题详情里把所属的里程碑显示成一个 chip，想知道「这一版还剩多少」
就得回网页端。

现在「更多」里加了**里程碑**，议题筛选栏里也有一枚 chip。看得到进度条、到期日，以及
染红的「已逾期 N 天」；点进去是该里程碑下的全部议题，可以逐条勾选。

> 一处语义要说明白：GitHub 没有「完成某个议题」这种接口，**勾掉就是把它关闭**。面板
> 里写明了这一点，免得以为是「标记完成但不影响状态」。拉请求同理——勾它只会关闭，
> 不会真的合并。

新建 / 编辑 / 结束 / 删除都有。

### 四、Gist 可以创建了

浏览一直有（「我的 Gist」在侧栏和用户页），缺的是创建。现在列表页右下角有 Fab，
详情页菜单里（自己的 Gist）有编辑 / 删除。

「私密」要讲清楚：私密 Gist **不等于安全**，不进搜索，但拿到链接的人依然打得开。
这句提示写在面板里了。

多文件的 Gist 编辑时会先问要不要改第一个文件——App 的编辑器一次只改一个，多文件的
建议回网页端。

### 五、代码搜索：补一处门槛说明

代码搜索一直能用，但 GitHub 的 Code Search **只对已登录用户开放**，匿名调用直接 401。
以前点「代码」页签会得到一个笼统的错误，现在未登录点击会先说明这件事，并给
「去登录」的口子。

### 顺带：三项用户以为缺、其实已有的功能

| 功能 | 在哪 |
|---|---|
| PR 评审闭环（批准 / 请求修改 / 逐行评论） | PR 详情 → 右上角「评审」；文件改动页点任意一行加行内评论 |
| PR 合并（merge / squash / rebase + 合并后删分支） | PR 详情 → 右上角「合并」 |
| 代码搜索 | 搜索页「代码」页签 |

这三项本轮没动代码，只是确认可用。不过「有三个塞菜单里就等于没有」——下一轮可以把
「评审」「合并」提到 PR 详情页的操作栏上，跟「关闭」并列。

### 本版没做的

里程碑**只有清单和进度条**，没有网页上那种横轴为时间的图表——那需要另一套取数方式，
收益不抵复杂度。Projects（新版看板）在 REST 上已被 GraphQL 那一套取代，也不是补个
接口的事。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**（对源码文件算
整体 SHA-256 并内嵌，被改过就报出来）。本次源码指纹（`SRC_SHA256`）：

```
079bb8d4faf5e7d79e4ef0cc8deb305873b11b8f3be8af5bd9d9a28548a7bb3a
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.2.0.apk` |
| 大小 | 877,981 字节（约 857 KB） |
| SHA-256 | `98e7c0b4d08ec458ddd4e98380ce51b64220f1b21394463506d845247af8eab7` |
| versionName / versionCode | `1.2.0` / `1002000` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |

---

## githup v1.1.7（四处界面毛病：底栏溢出 / 列表冒出序号 / 收藏独占一行 / 排序栏折行）

### 修：底栏选中态溢出胶囊

选中项那圈玻璃环是「本体再向外扩一点」，可本体已经顶到内容区了
（胶囊 64 = 内衬 4 + 内容 56 + 内衬 4）。原来向外扩 6px，外环就有 68px ——
**比胶囊本身还高**，上下各穿出 2px；第一格还往左穿出 2px 压在圆角上，最扎眼。
改成 2px：外环 60px，比内容区仍凸出 4px（原设计那种「圆头比长条更高」的观感还在），
上下左右四个方向都留在胶囊里。

### 修：仓库列表从第 2 条开始凭空多出 1、2、3

```js
items.map(repoRow)          // 看着没传参数，其实传了三个
```

`Array.map` 送进去的是 `(元素, 下标, 数组)`，**下标正好落在 `repoRow` 第二个参数
`extra`（额外一行）上**。第 1 条下标是 0（假值，看不出来），第 2 条开始 1、2、3
就被当成一行文字渲染到条目下面了。

调用处改成显式写法，同时给 `repoRow` 加了道闸：`extra` 不是字符串就当没有 ——
以后再有人写成 `map(repoRow)` 也漏不出数字。

> 顺带说明：探索页「趋势榜」左边那列 1、2、3 是**刻意的名次**，跟上面这个
> 不是一回事，这次没动。

### 修：Star 列表的「收藏」单独占一行

`repoRow` 的 `extra` 会被放进 `row-main` 里当**新的一行**，收藏按钮走这条路，
等于每条下面再挂一行，整张表每条都鼓出来一块。

现在给 `repoRow` 加了 `opt`：「Star 于 x 天前」并进已有的 meta 那一行
（和语言 / 星数 / 更新时间同排），收藏按钮去行尾的操作区（竖着居中、不占整行）。
条目高度回到正常。

### 修：排序那一栏会折行

排序段控件 + 收藏夹段控件并排一行，窄屏上 flex 会把按钮压窄，中文就折成两行 ——
「收藏夹」竖着排成三个字。现在按钮只许撑开不许折断，整行撑不下就横向滑一下
（滚动条隐藏），不再折断也不再挤到下一行。

### 顺带：1.1.6 里的 Star 排序改动也在这个包里

上一版把「我的 Star」三档排序理顺了，这次一并带上：

| 档位 | 排序字段 | 方向 |
|---|---|---|
| Star 时间 | 这个仓库被 Star 的时间 | 从小到大 |
| 最近更新 | 卡片上显示的那个时间 | 从大到小 |
| Star 数 | 星数 | 从大到小 |

「最近更新」这一档原来直接沿用服务端顺序，而服务端实际按 `pushed_at` 排、
卡片却显示 `updated_at` —— 两个字段不一致时，屏幕上会出现「26天前 排在 14天前
前面」，看着像排序坏了。现在按显示那个字段重排，保证那串时间单调递减。

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.1.7.apk` |
| 大小 | 868,121 字节（约 848 KB） |
| SHA-256 | `1ea5ccb7340068161490723e53054ccecb05c9810917fba72fde637ace0f6429` |
| versionName / versionCode | `1.1.7` / `1001007` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |
| 签名证书 | `CN=githup, OU=Buwrt, O=Buwrt`，SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927` |

签名与本库以往各版本 **完全相同**，可直接覆盖安装，不用卸载。

----

## githup v1.1.6（新手引导改成「边做边学」/ 藏起来的按钮和手势逐个讲透）

### 新：新手引导不再只是「看一遍点下一步」

上一版引导是十步纯展示：把该看的地方一个一个圈出来，配一张说明卡片，
看完点「下一步」就算完。真机上看下来有个明显的问题 —— **界面上没有任何
提示的那些功能，引导也没能教会**。长按、拖动、下拉这类手势，用户压根
不知道那里可以按，圈出来告诉他「这里能长按」，他看完还是不知道怎么用、
什么时候该用。

这一版改成**边做边学**：16 步里有 6 步要你真的动手，做对了自动往下走，
做不出来点「跳过这步」也能过去。

| 步骤 | 要你做的动作 | 做完你就知道了 |
|---|---|---|
| 右上角 | 点一下那枚收纳箱图标 | 那是下载管理，Release 包的进度都在这儿看 |
| 底栏 | 按住胶囊左右拖一段 | 底栏能拖着切页，指示条跟手走 |
| 页面顶部 | 按住往下拖一大段再松手 | 没有刷新按钮，下拉就是刷新 |
| 译字按钮 | 按住不放约 1 秒 | 长按出菜单：换引擎、自动翻译、清缓存 |
| 通知列表 | 按住任意一条不放 | 长按进多选，底部浮出批量操作栏 |
| 设置 | 点一下「重置新手引导」 | 引导可以重播，也可以现在重看 |

做的时候遮罩**只放开这一步的目标**：点别的地方一律被拦下并给一句轻提示，
不会误触跳到别的页面去。而点/长按到目标上时，真实功能照常发生 ——
长按「译」按钮会真的弹出菜单，这时遮罩临时退到菜单后面，让你把弹出来的
东西看清楚，一秒多之后自动收掉再进下一步。下拉刷新的判定阈值（75px）比
真实刷新（70px）略大，保证「页面真的刷了」在先、「这一步做对了」在后，
不会出现照着做了却什么都没发生的错觉。

### 讲透那些藏起来的功能

除了上面 6 个要动手的，文案里也把以前一句带过的功能说清楚了：

- **右上角收纳箱 = 下载管理**：常年待在右上角、没有文字，是最容易一直没被发现的入口
- **翻译引擎列表里长按某条 = 用浏览器打开它的官网控制台**去拿密钥（百度翻译 / 有道开放平台 / 小牛翻译），密钥只存在本机
- **探索页的语言标签可以左右滑**
- **通知条目长按 = 多选批量删**（点一下是打开，长按才是多选）
- 结束页列了一张「刚才这些藏起来的」速查表

引导版本号从 1 升到 2，**看过旧版引导的老用户也会重新走一遍**；
不想看随时点右上角「跳过」，设置里照常可以重看和重置。

### 顺手修掉的几个坑

- **气泡和「跳过」按钮被弹层盖住**：它们原来挂在 `#ob-root` 里面，而
  `position: fixed` 的元素自成层叠上下文，子元素永远逃不出去 —— 遮罩一退到
  弹层后面，气泡就跟着被盖住，既看不到「✓ 就是这个」也点不到「跳过」。
  现在挂在 `body` 上，和遮罩做兄弟节点，各自的 `z-index` 各算各的。
- **做完第二次就不灵了**：下拉刷新、点设置项都会 `Router.reload()` 把整片
  DOM 换掉，挂在元素上的监听会随旧节点一起被扔掉。现在监听一律挂
  `document` 做委托，用 `closest` 判定，重渲染多少次都不影响。
- **没数据就卡住**：未登录的通知页一条通知都没有，长按无从谈起。目标 2.5 秒
  等不到就自动退回普通展示步骤，「下一步」照常可点，绝不把人卡在遮罩里。

### 改：「跳过」收进引导卡片，不再挡着右上角的图标

引导卡片右上角原来常驻一枚「跳过」胶囊，它和页面的**菜单、下载管理、翻译**
三个按钮正好在同一条水平线上 —— 而这一段引导恰恰要求用户去点那里
（「点一下右上角那枚收纳箱」）。真机上反馈「有点挡」。

现在它收进说明卡片的按钮行最右边，和「上一步 / 下一步」排在一起：

```
常态        [上一步]                        [下一步] [跳过]
动手步骤    [跳过这步]                                [跳过]
```

「跳过」做成描边药丸（次要动作），不去和实心蓝的「下一步」抢注意力；
右下角的位置照旧一眼能找到，系统返回键在任何一步也仍然等同跳过。

一并把相关文案改口：欢迎页里「点右上角跳过」改成「点卡片右下角的跳过」，
乱点时的轻提示和手动重播时的提示也同步改了。

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.1.6.apk` |
| 大小 | 866,221 字节（约 846 KB） |
| SHA-256 | `79e6785d303a81f91cf7c460fa0b4ccecb101506beb6a6c3d2e34b359ba5a7c1` |
| versionName / versionCode | `1.1.6` / `1001006` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |
| 签名证书 | `CN=githup, OU=Buwrt, O=Buwrt`，SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927` |

签名与本库以往各版本 **完全相同**，可直接覆盖安装，不用卸载。

---

### 新：代码页能切换分支 / 标签 / 老版本

以前打开一个仓库只能看默认分支，想看别的分支、想翻某个老版本的代码，没有入口。

**切换按钮现在文件页也有。** 原来只有目录页面包屑右侧那枚，而且长得就像「显示一下
你在哪个分支」的标签，不像能点；进了文件之后它还会消失 —— 想换分支得退两级回目录页，
切完再一路点回来。现在文件页顶部也放了同一枚。

**切换后留在当前路径。** 在 `src/main.js` 上切到 `v1.1.5`，切完还停在 `src/main.js`
上，不会被丢回仓库首页。

**选单本身也重做了**，原来只是一个 100 条的平铺列表：

| 场景 | 原来 | 现在 |
|---|---|---|
| 分支多的仓库 | 前 100 条平铺，找不到 | 有搜索框，输入即过滤 |
| 超过 100 条 | 后面的永远看不到 | 一页 100 条，点「加载更多」继续 |
| 列表里没有的老版本 | 选不到 | 能手动填分支名 / 标签名 / 提交 SHA |
| 有没有版本 | 看不出来 | 分段直接标数量：「分支 (2)」「标签 / 版本 (6)」 |
| 空列表 | 一律一个空列表 | 分三种说：还在加载（转圈）／真没有／拉失败 |
| 哪个版本新 | 只有一串短 SHA | 标签行给出「3 天前」「1 个月前」 |

另外看的不是默认分支时，面包屑下方会提示「正在看 xxx，不是默认分支」，
点一下就回默认分支 —— 切到老版本后目录长得不一样，不说清楚容易被误判成
「这仓库怎么少了一堆文件」。

### 修：带斜杠的分支名切过去找不到

`feature/login`、`release/v1.2` 这类名字，原来塞在 URL 路径里只能靠 `%2F` 撑着，
而**部分 WebView 会把 `%2F` 解码回 `/`**，ref 只剩第一段 `feature`，后半截变成
文件路径。现在 ref 改走 `?ref=` 查询参数，不参与路径切分；旧格式链接继续认，
不失效。

### 修：空状态一直没有图标

octicon 的 `inbox` 图标漏了，而它是 `UI.empty()` 的默认图标 —— 所有空状态
本该显示图标的地方其实都是空的。已补上。

签名与本库以往各版本 **完全相同**，可直接覆盖安装，不用卸载。

---
## githup v1.1.5（选中项只留外圈 / 精简设置页说明 / 仓库页页签吸附修复 / 新手指导可重置 / 设置项改名「iOS风格」/ 顶栏标题靠左 / 搜索按钮放大 / 全机型适配）

### 改：iOS 风格下选中项只留外圈，不要里面那个圈

底栏选中项原来是**两层叠在一起**：外面一圈近白半透明的玻璃环，里面还有一块浅灰紫的实心圆。
在电脑上放大看是「有层次」，到手机上就是**一个圈里面又套了一个圈**，看着像没对齐的双层描边。

现在把内圆去掉，只留外面那圈：

```
改前   内圆（浅灰紫实心）+ 外玻璃环   →  看着是「两个圈」
改后   只有外玻璃环                  →  一圈半透明白，玻璃感还在
```

改法就一处 —— 内圆是 `.tab-ind` 本体的 `background`，置 `transparent` 即可：

```css
--nav-indicator: transparent;   /* 内圆不画了（浅色 / 深色各一处） */
--nav-indicator-edge: none;
```

**外圈的尺寸、圆角、外扩量一个都没动**，所以最终视觉尺寸和原来那圈外环完全一致：

| 项 | 改前 | 改后 |
|---|---|---|
| 内圆底色 | `rgba(233,231,239,.96)` | `transparent` |
| 内圆阴影 | 4 层（含 1px 描边） | `none` |
| 外环底色 | `rgba(252,252,254,.6)` | 不变 |
| 外环外扩 | `-6px` | 不变 |
| 外环圆角 | `999px` | 不变 |

深色同理：内圆原来是 `rgba(56,54,74,.85)` 的深紫块，去掉之后只剩那圈 `rgba(255,255,255,.09)`
的浅色薄雾 + 亮边 —— 面本来就暗，这圈亮边就是轮廓的全部来源，去掉内圆反而更干净。

**几何一点没动**（这是最要紧的一条 —— 指示器的落位是 JS 按 `(栏宽-8)/格数` 算的，
很容易被「顺手改一下尺寸」搞歪）。五个页签实测落位：

```
step = 70.6px
home 0.0 ✓   notifications 70.6 ✓   explore 141.2 ✓   search 211.8 ✓   profile 282.4 ✓
拖动跟手、松手吸附、按压时的 scale(1.06,.9) 回弹 —— 均与改前一致
朴素模式（classic）不受影响：两边本来就都是透明的
```

### 修：iOS 风格下仓库页的页签漂浮在内容中间

iOS 风格里进仓库页往下滚，「代码 / 议题 / 拉取请求 / Actions」这一行**不吸在顶栏下面**，
而是悬在半空，看上去像夹在文件列表中间。朴素模式是正常的 —— 用户拿这两种一比就看出来了。

```
iOS 风格   页签停在 y=104，顶栏底边 y=52  →  悬空 +52px   ✗
朴素模式   页签停在 y= 53，顶栏底边 y=53  →  贴合  ±0px   ✓
```

根因：**同一个 52px 让了两次**。页签是 `position: sticky`，而 sticky 的 `top`
是相对滚动容器 `#view` 的 padding box 算的；`#view` 自己已经有
`padding-top: calc(appbar-h + safe-t)` 把顶栏位置让出来了，
再给页签一个同样的 `top` 就是让第二遍，吸附点跑到「顶栏底 + 52」。
朴素模式恰好是因为 `#view` 没有那个 padding，基础规则 `top: 0` 才对得上。

所以删掉那条多余覆盖即可 —— **两种风格的正确取值都是 0**。
`position: sticky` 的搜索栏同病，一并处理。

修完后滚动实测，页签与顶栏底边的偏差：

```
iOS 浅色 / 深色 ±0px      朴素 浅色 / 深色 ±0px
魅族20Pro / 魅族20 / 320×569 / 480×1040 / 横屏 / 平板 —— 六种机型 × 两种风格全部 ±0px
```

回归：10 档机型 × 两种风格无横向溢出，新手引导、iOS风格 开关、搜索按钮均与基线一致。

### 改：删掉设置里两段多余的说明文字

设置页原来在分组下面挂几行小灰字说明，其中两段删掉了：

- 「启动页决定应用打开时默认显示的标签页。iOS风格关掉后会回到朴素的贴底样式，
  适合在玻璃效果卡顿的机型上使用。」
- 「十步带你认全顶栏、底栏、搜索、探索和设置里的关键开关。看完或跳过之后不会再自动弹出：
  新手指导是现在立刻再看一遍，重置新手引导是抹掉「已看过」的记录，下次打开 App 时自动重播。」

这两段讲的都是上面那几行自己已经写出来的东西：开关本来就标着
「开启：底栏是悬浮的磨砂胶囊 / 关闭：底栏是贴底的朴素样式」，启动页标着当前是「首页」还是别的，
两行又各带状态值（「已完成 / 未开始」、「可重置 / 已是初始状态」）。注了等于没注，
只是把设置页铺成一大片灰字。真正有信息量的两条（版本号更新规则、登录后能解锁什么）**保留**。

设置项一个没少，仍是 11 行。两行功能的区别也不会因此含糊：
「新手指导」点下去立刻从头播一遍，「重置新手引导」只清记录，下次打开自动播。

真实页面量出来的差别（393×852）：

```
改前   11 行 + 4 段灰字（246px 高），整页 1017px
改后   11 行 + 2 段灰字（ 95px 高），整页  852px
说明区少了 151px，整页少了 165px —— 这个尺寸下正好一屏装得下，不用滚
```

保留的两段是真正有信息量的：版本号更新规则、登录后可解锁什么。
魅族20Pro / 魅族20 / iPhoneSE 三档行数都是 11、都无横向溢出；
最窄那档还要下滚 259px 才到底，滚到底时最后一行离底栏还有 8px —— 没有被底栏压住。

文件名 `githup-1.1.5.apk`，对外版本号 **1.1.5**（versionCode `1001005`），应用名 **githup**，
包名 `com.hubmobile.app`，Android 7.0 起可用。
签名与本库以往各版本 **完全相同**（证书 SHA-256 `863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927`），
可直接覆盖安装，不用卸载。

### 新：详细新手指导，十步高亮，随时可跳过

第一次打开自动播放，像游戏新手教学那样 **把该点的地方一个一个圈出来**，
整屏压暗、只留一个洞亮着，旁边配一张说明卡片：

| 步 | 圈哪里 | 说什么 |
|---|---|---|
| 1 | 全屏 | 欢迎使用 githup（不圈任何控件，居中卡片） |
| 2 | 顶栏 | 这里是页面标题栏，标题统一靠左 |
| 3 | 右上角 | 这里的按钮随页面变化，比如搜索、更多 |
| 4 | 底部 | 五个页签在这里切：首页 / 通知 / 探索 / 搜索 / 我的 |
| 5 | 搜索框 | 输入关键字，回车或点按钮开始搜（自动切到搜索页） |
| 6 | 搜索类型 | 仓库 / 用户 / 代码，在这排页签之间换 |
| 7 | 探索时间范围 | 今天 / 本周 / 本月，决定榜单的时间窗口（自动切到探索页） |
| 8 | 探索语言 | 选语言过滤榜单，只关心某一种语言时用（自动切到探索页） |
| 9 | 设置 → iOS风格 | 玻璃风格不喜欢可以在这里关掉（自动切到设置页） |
| 10 | 全屏 | 全部介绍完，开始使用 |

几个不是「做了就行」、而是踩过坑才定下来的细节：

- **每一步都可以跳过**：右上角常驻「跳过」，返回键也能直接退出，不用硬看完。
- **看过了就不再弹**：状态记在本地，只播一次；**设置 → 新手指导** 里可以随时重看，
  那一行还会写「已完成 / 未开始」，点一下从头再播一遍。
- **洞是真的挖在控件上**：按目标元素的实际位置实时计算，目标还没渲染出来就轮询等，
  等不到就退化成居中卡片，而不是画一个空洞。
- **按钮太小、位置太低、屏幕太窄** 这三种情况都会自动换边（洞下方放不下就放上方，
  再不行就居中），并且永远避开右上角的「跳过」按钮。
- 跳过或看完都会把你送回开始引导前的那个页面，不会停在某个陌生页面上。

### 新：新手引导可以在设置里重置

设置里新增 **重置新手引导**：点一下就把引导恢复成「没看过」的状态，
**下次打开 App 会自动重新播放一遍**（不立刻弹，避免和「现在就看」混淆）。
旁边原有的 **新手指导** 一行是「现在立刻再看一遍」，两者分工不同：

| 入口 | 做什么 | 什么时候弹 |
|---|---|---|
| 新手指导 | 清掉记录 + **立刻**从头播一遍 | 立刻 |
| 重置新手引导 | 只清掉记录，**不**立刻播 | 下次打开 App 时自动播 |

重置后设置页的两行会当场刷新（「可重置 → 已是初始状态」、「已完成 → 未开始」），
点完就能确认生效；如果本来就是没看过的状态，会提示「已经是初始状态」而不是空点一下。

### 改：设置里的「液态玻璃底栏」改名为「iOS风格」

开关的行为、默认值（开）、`data-s="glass"` 都没动，只是把名字换成用户实际叫它的那个。
引导第 9 步的文案与高亮目标也跟着同步。

### 改：顶栏标题改回左对齐

首页 / 通知 / 探索 / 搜索 / 我的，**五个页签的顶栏标题统一靠左**。
上一版按 iOS 习惯居中了，但中文标题居中在这套顶栏里读起来是散的，改回左对齐。

### 改：搜索按钮、顶栏按钮都放大了

搜索按钮原来被挤得很小，点起来要瞄。现在输入框和按钮都提到 **40px 最小高度**
（玻璃主题下按钮 44px），顶栏图标按钮 36 → **40px**，
按钮加了 `flex: none` —— 否则旁边输入框一变长就会把它压扁。

### 改：适配市面上所有机型（含魅族这类窄屏和挖孔屏）

| 适配点 | 做法 |
|---|---|
| 左右安全区 | 新增 `--safe-l` / `--safe-r`，横屏和挖孔屏不再被切边；由原生层注入真实内边距 |
| 高度基准 | `100dvh` 优先，`100vh` 兜底，老 WebView 也不会出现底部空白 |
| 窄屏 ≤360px | 底栏间距与抬升量单独收敛 |
| 大屏 400~719px | 单独一档间距 |
| 横屏（高 ≤520px） | 顶栏压到 44px、底栏 52px，别把内容挤没 |
| 系统字体缩放 | 锁 `text-size-adjust: 100%`，防止用户放大字体后布局炸开 |
| 老 WebView 没有玻璃 | 检测到不支持 `backdrop-filter` 就自动降级成近实色，不会出现半透明的脏斑 |

### 此前同一个 1.1.5 里已经改好的

- **悬浮液态玻璃胶囊底栏**：按样板逐像素量出「外圈压暗、顶部提亮」的三段结构重建，
  不再是描一圈白边。
- **设置里可以关掉玻璃**：`设置 → iOS风格`（原「液态玻璃底栏」），默认打开，关掉回到贴底的朴素样式。
- **下载加速通道 3 条 → 5 条**，按实测速度重排（原来按知名度排，实测恰好反着）。
- **下到残缺文件不再报「下载完成」**：发车前先问大小，完成时比对，对不上换道重下。
- **点了更新却没装成、之后不再提醒**：加上安装回执 + 30 分钟有效期，旧包写的记录一律按过期处理。

### 代码自校验（不需要服务器）

包内自带两道：**签名自校验**（证书指纹不符直接拒绝运行）+ **源码指纹**
（对源码文件算整体 SHA-256 并内嵌，被改过就报出来）。
本次源码指纹（`SRC_SHA256`）：

```
de56861c70904f3592c296fb7aaff2e46576283291e43685112be92f682527ec
```

### 安装信息

| 项 | 值 |
|---|---|
| 文件名 | `githup-1.1.5.apk` |
| 大小 | 813,528 字节（约 795 KB） |
| SHA-256 | `c2dfb031c50212f3a736a9fbd11b87a1a848e9d2cdece3ef2641bff321d1f2de` |
| versionName / versionCode | `1.1.5` / `1001005` |
| 应用名 / 包名 | githup / `com.hubmobile.app` |
| 最低系统 | Android 7.0（API 24） |
