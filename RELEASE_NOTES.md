## githup v1.1.4（更新提示不再反复弹 / 返回键回到该回的地方）

文件名 `githup-1.1.4.apk`，对外版本号 **1.1.4**（versionCode `1001005`），Android 7.0 起可用。
签名与本库以往各版本 **完全相同**，可直接覆盖安装，不用卸载。

这一版集中修三个「点了没用、退了不对」的问题：更新提示点完还一直弹、
从内页退不回来、标签页按返回把 App 关掉。

> **关于版本**：对外版本号与内部号都保持 1.1.4 / `1001004`，不做变动。
> 这个版本号下先后出过几个内容不同的包，更新检测靠**安装包指纹**（不是版本号）
> 来区分 —— 弹窗会直接写「这个安装包已更新」并列出两份包的指纹，装上去就是最新那份。

---

### 修：点了「立即更新」，更新提示还一直弹

**现象**：弹出「有新内容可用」，点「立即更新」，下载也开始了 ——
但切回前台或重开一次 App，同一个提示又弹出来，反反复复。

**根因**是「本机指纹」和「期望指纹」之间存在一段必然对不上的空窗期：

| 名字 | 含义 | 什么时候变 |
|---|---|---|
| 期望指纹 | 服务端那一份包的 SHA-256 | 服务端一换包立刻变 |
| 本机指纹 | 已安装 APK 的 SHA-256 | **要等新包真装上才变** |

点「立即更新」之后，中间隔着「下载 → 拉起系统安装器 → 你在安装器里点确认」。
这段窗口里本机指纹**仍指向旧包**，可每次切回前台都真的重查一遍，
一查必然「不一致」，于是再弹一次 —— 循环往复。

**改法**是让「立即更新」真的算数：点下去、且确实开始下载了，
就把那一份的指纹记成「正在装」，装完之前不再提醒。

- **只记指纹，不记版本号**：指纹 = 「是哪一个包」，同一个包不会装两遍；
  版本号 = 「哪一代」，同一代可能被重新打包 —— 那是个全新的包，指纹对不上，照常提醒
- **没真的开始下载就不记**：没有 APK 附件 / 内置表与清单冲突 / 拿不到指纹
  这三种会退化成「打开官方下载页」，记了反而会把后续提醒一起压住
- **强制更新不吃这套**：第一、二位版本号变化时不认「跳过」也不认「正在装」
- **装上之后自动失效**，不留残渣；卸载重装、清数据后记录一并消失

### 修：从「探索」页进「下载管理」，按返回被甩回首页

**现象**：在「探索」页点右上角下载图标进下载管理，按返回没有回到探索页，
而是直接跳到了首页。

**根因**是标签根页面名单 `TAB_ROOTS` 里混进了 `/downloads` 和 `/login`。
它们是从右上角入口或流程里进去的**内页**，底部导航栏上没有它们的位置，
但被错当成「一级标签」后：

1. 进下载管理被判定为「标签之间横向切换」，走 `replaceState` —— 不堆历史、深度不加
2. 按返回时没有可退的层级，落到「停在标签页上 → 回首页」的兜底

**改法**是让名单名副其实 —— 判据改成「底部导航栏上有没有它的位置」：

```js
var TAB_ROOTS = ['/', '/notifications', '/explore', '/search', '/profile'];
```

现在从任何页面进下载管理都算真的深入一层，按返回退回**进来前的那个页面**。
顺带把 `isTabPath()` 里抄的第二份名单也改成复用 `TAB_ROOTS` ——
两处各写一遍、改一处漏一处，正是这类问题的温床。

### 改：在任意标签页按返回，回到首页而不是退出应用

以前在「通知」「探索」「搜索」「我的」上按返回，因为没有上一层可退，
直接落到「再按一次退出」—— 稍微多按一下 App 就没了。

现在这些标签页上按返回先切回首页，只有在**首页**再按返回，才进入「再按一次退出」。

返回键的完整优先级：

| 顺序 | 拦到谁 | 动作 |
|---|---|---|
| 1 | 图片查看器 | 关掉查看器 |
| 2 | 弹层 / 确认框 / 菜单 | 关掉弹层（强制更新弹层除外，它要吞掉返回键） |
| 3 | 纵向层级（详情 / 议题 / Release…） | 逐级退回上一层 |
| 4 | 停在某个底部标签上 | 回到首页 |
| 5 | 已经在首页 | 交给「再按一次退出」 |

---

**官方签名证书指纹（SHA-256）**

```
863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927
```

校验方式：

```bash
apksigner verify --print-certs githup-1.1.4.apk
```

---

### 更新方式

打开软件即自动检测。1.1.3 → 1.1.4 是第三位变化，属于**可选更新**，
弹窗会给「立即更新 / 稍后提醒 / 跳过此版」。也可以「设置 → 检查更新」手动触发。

### 校验

- 大小：770,193 字节
- SHA-256：`19394c735a9e0acfaf2678ea770b903d764ebf508c27af5c78e8e3aae75b65fb`

---

每个版本改了什么都记在 [CHANGELOG.md](https://github.com/Buwrt/githup/blob/main/CHANGELOG.md)。

## githup v1.1.3（相册视频 / 搜索筛选 / PR 评审）

文件名 `githup-1.1.3.apk`，包内版本号 **1.1.3**（versionCode `1001003`），Android 7.0 起可用。
签名与上一版 **完全相同**，可直接覆盖安装，不用卸载。

这一版补三件事：相册里的**视频**能选了、搜索有了**排序和筛选器**、拉取请求可以**评审和合并**。

> 这批改动原本发的是 1.1.4，但安装包被官方校验拦下（身份常量与包内版本对不上）。
> 根因是加固脚本里版本号手写、发版时漏同步，**与功能无关**，已改为自动读取并改回 1.1.3。

---

### 新增：搜索结果可以排序、可以筛

搜索结果页顶部多了一条工具栏：

- **排序**：仓库可按 Star 数、Fork 数、更新时间排序；议题可按创建时间、评论数、点赞数排序；提交按提交时间
- **筛选**：语言 / Star 数 / Fork 数 / 最后推送时间 / 开源许可 / 是否已归档，
  面板底部实时显示拼出来的搜索式，确认无误再应用
- 结果区上方标出当前排序和实际发给 GitHub 的搜索串，方便核对
- 用户、代码、话题这三类 GitHub 不支持排序，就不显示排序入口，免得点了报错

---

### 新增：拉取请求可以评审、可以合并

**评审** —— 批准 / 请求修改 / 仅评论三种结论；
在「文件改动」里点任意一行可写**行内评论**，攒着随评审一起提交（和网页端一致）；
会话页顶部能看到谁批准了、谁要求改。

**合并** —— 创建合并提交 / 压缩合并 / 变基合并三选一；
合并前提示冲突、分支保护、分支落后等状态；草稿和已关闭的 PR 直接拦下；
**合并后可自动删除源分支**（fork 过来的分支和目标仓库默认分支不给删）。

---

### 修：相册里的视频选不了

图片能选、视频却要进文件夹翻 —— 是我上一版写窄了：同时要图片和视频时跳过了相册。
现在按类型分流，图片、视频、图片+视频都直接进系统相册，只有 APK / 压缩包才走文件浏览器。

---

## githup v1.1.3

文件名 `githup-1.1.3.apk`，包内版本号 **1.1.3**（versionCode `1001003`），Android 7.0 起可用。
签名与 1.1.2 **完全相同**，可直接覆盖安装，不用卸载。

这一版把「网页能翻译、App 里翻不了」这件事解决了，顺带让平板 / 折叠屏能用得舒服些，
并按议题修掉了搜索翻页、返回位置、视频图片这三个问题。
另外补上了之前一直没有的**插入图片 / 视频**：写评论、提 Issue 时可以插图录屏了。

---

### 新增：评论和 Issue 可以插图 / 视频

以前这两处只能写纯文字，现在工具栏多了一个**图片按钮**（在「引用」旁边）：

- 点一下从相册选图或选视频，**自动上传到 GitHub**，传完把链接插在当前光标处
- 图片插 `![](链接)`，视频插裸链接，发出去在 GitHub 网页端显示效果一致
- 支持格式：`png / jpg / gif / webp / mp4 / mov / webm` 等常见图片与视频，单个上限 **25MB**
- 上传期间按钮会禁用并显示「上传中」，不会重复点；失败会提示具体原因（没登录 / 文件太大 / 仓库不支持附件）

技术上的两处讲究：

- 文件是**边读边发**的，不会整个塞进内存 —— 传 25MB 的视频也不会把 App 撑崩
- GitHub 返回的附件地址**没有扩展名**，渲染时走已有的「视频 → 图片 → 链接」降级链，
  所以不存在「传上去了但显示不出来」的情况

---

### 新增：翻译本页

浏览器上的 GitHub 能用扩展翻译，App 里的 WebView 装不了扩展、也注入不了脚本 —— 所以翻译直接做进了 App 自己。

- 右上角**地球图标是总开关**，默认关闭。打开后当前页翻成中文
- **总开关是全局的**：只要不关，换到哪个页面都接着翻，不用每页重新点
- 关掉即还原原文，不重建页面、不重新请求接口

**滚动驱动**：只翻屏幕上下各 600px 范围内的英文段，你滚到哪译到哪，屏幕外的内容不翻。
请求量小、上屏快，也不会因为整页几十段一起发请求而撞上引擎限流。

自动跳过不该翻的内容：代码、仓库名、用户名、`@某人`、版本号、文件路径。

---

### 翻译引擎：全部免费，开箱即用

不用注册、不用 Key。默认**自动探测**，谁先通就用谁，结果记下来不重复试。

| 引擎 | 特点 |
|---|---|
| **有道** | 国内直连最稳的免费引擎，无需 Key |
| **设备端翻译** | 系统内置，**离线、不联网、文本不出设备**（需装中英语言包） |
| **DeepL** | 质量最佳，公共 IP 偶尔限流 |
| **Google** | 老牌免费接口，需海外网络 |
| **MyMemory** | 兜底，全球可达，匿名有日配额 |
| ~~微软 Edge~~ | **已停用**：微软关闭了公开令牌接口，实现保留但已移出探测链 |

设置页可手动指定引擎。某家挂了会自动换下一家，不会把你晾在英文页上；
某几段没翻出来会隔 4 秒自动重翻，最多 5 轮。译文缓存在本机，同一段不重复请求。

> 国内手机上通常只有**有道和 MyMemory** 能用，其余的（Google / DeepL）需要海外网络 ——
> 这是网络环境决定的，不是软件的毛病。微软 Edge 是厂商关停，实测
> `edge.microsoft.com/translate/auth` 现在直接返回 404。

---

### 修：打开翻译后，页面加载变慢（骨架屏一直转）

翻译本身不慢，是它**把 App 的网络线程池占满了**。

页面数据请求和翻译请求共用 `JsBridge` 里那一个线程池，原来只有 4 个线程。
自动选引擎时并行探测 5 家，连不上的（Google / DeepL）每个都要挂满 20 秒连接超时才放弃 ——
4 个线程瞬间占满，页面自己的请求全堵在后面。而且探测失败没有记忆，每换一页重来一遍。

三层修复：

| 层 | 改动 | 效果 |
|---|---|---|
| 线程池 | 4 → **8** 线程 | 翻译与页面数据各走各的，不互相堵 |
| 连接超时 | 20s → **12s** | 连不上就早点放手，不白占线程 |
| 探测记忆 | 失败引擎 10 分钟内不再试；一轮全败后 **2 分钟冷却** | 换页不再反复重跑探测 |

冷却期内换页照样能翻，只是不再做那轮"谁最快"的竞速。

### 修：翻译请求在真机上其实一次都没走原生通道

这个 bug 藏得比较深，症状是「**只有 MyMemory 和有道能用，Google / DeepL 全部失败**」，
而且每次翻译都慢半拍。

请求链路是两层，很容易转错手：

```
translate.js ──▶ window.Native.http(...)        ← api.js 的 JS 封装，收「对象」
                 （它内部会 JSON.stringify）
                 ──▶ NativeBridge.http(..., headersJson)  ← Java，收「字符串」
                     new JSONObject(headersJson)
```

问题出在 translate.js 里先 `JSON.stringify` 了一次，到 Java 手上是「JSON 字符串的字符串」，
`new JSONObject` 直接抛异常 → 请求以 `status 0` 失败 → 翻译只能退回 WebView 的 fetch 兜底。
fetch 受跨域限制，于是**只有服务端愿意给 CORS 头的引擎（有道 / MyMemory）能用**，
其余全灭；而且每个请求都要「原生失败一次 + fetch 重试一次」，白白多花一倍时间。

现在只 stringify 一层（把对象交给 api.js 去转），原生通道恢复工作：
不再依赖对方给 CORS 头，请求也不用失败重试一遍。

### 修：搜索结果只能看第一页（议题 #1）

搜索接口只请求了第 1 页（`per_page=30`，没传 `page`），界面上也没有翻页入口 ——
所以不管实际有多少结果，你永远只能看到那一屏，看起来像「搜索坏了」。

现在底部有**「加载更多」**，表头会写明「共 N 条结果（已加载 M 条）」。
GitHub 搜索本身最多返回 1000 条，翻到顶会明确提示，不再让人干等着。

### 修：搜索结果点进详情再返回，会重新加载并回到顶部（议题 #2）

路由渲染时无条件 `innerHTML=''` + `scrollTop = 0`，按返回键也照做一遍，
结果就是「翻了半天 → 点进去 → 回来从头开始」。

现在按路由记住滚动位置，返回时原样还原；搜索结果按「类型 + 关键词」缓存，
返回时直接把上次的结果画出来，**不再重新请求接口**。

### 修：issue / README 里的视频和图片看不了

两件事，原因完全不同：

**视频**：Markdown 渲染器把所有原始 HTML 一律丢掉，而 GitHub 上传的视频在正文里
就是裸的 `<video src="…">` 标签 —— 于是它连个影子都没有。现在放行一小撮安全的
排版 / 媒体标签（后面仍有 DOMPurify 兜底，script / iframe 一概留不下），
裸的 `.mp4 / .mov / .webm / .m4v` 链接也会就地变成播放器。
另外补了 `controls`：GitHub 贴进来的标签未必自带这个属性，缺了它就是一块按不动的黑砖。
全屏播放和「返回键先退出全屏」也接上了。

**图片**：外链图片被渲染成 `<a target="_blank"><img></a>` 而且不带 `data-zoom`，
内置查看器只认 `data-zoom`，WebView 又没开多窗口、也没实现新窗口回调 ——
点下去既不放大也不跳转，等于「图片点不了」。现在内外链图片一律可点开看，
走 App 内置的查看器；加载失败的给一个虚线框，不再只剩一片空白。

---

### 新增：平板 / 折叠屏适配

- 不再锁死竖屏：平板横持、分屏、折叠屏展开都跟随系统旋转
- 转屏**不重建 Activity** —— 登录态、翻译开关、滚动位置都不丢
- 宽屏下内容限宽居中（900px），底部标签栏不再被拉成 1280px 平分
- 横屏时左右避开刘海 / 挖孔

---

### 沿用 1.1.2 的安全加固

防护链、R8 混淆、装包前证书校验全部保留，前端加文件后会用官方私钥重签防护链常量。

> **注意**：带加固的安装包是从 [Buwrt/githup](https://github.com/Buwrt/githup) 构建的。
> 本库按设计不含 `Guard` / R8 配置，直接在这里 `assembleRelease` 出来的包**没有防护链** ——
> 用它做调试对照可以，不要拿来当发布包。

**官方签名证书指纹（SHA-256）**

```
863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927
```

校验方式：

```bash
apksigner verify --print-certs githup-1.1.3.apk
```

---

### 更新方式

打开软件即自动检测。1.1.2 → 1.1.3 是第三位变化，属于**可选更新**，
弹窗会给「立即更新 / 稍后提醒 / 跳过此版」。也可以「设置 → 检查更新」手动触发。

### 校验

- 大小：742,733 字节
- SHA-256：`f27b5f5842ed5c751f3960f9ac8a551c90f83878378cd194582b20a24aebaf68`

---

从这一版起，每个版本改了什么都记在 [CHANGELOG.md](https://github.com/Buwrt/githup/blob/main/CHANGELOG.md)。
