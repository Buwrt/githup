## githup v1.1.2

文件名 `githup-V5.apk`，包内版本号 **1.1.2**（versionCode `1001002`），Android 7.0 起可用。

### 修复

**已是最新版本，却弹出「立即更新」的强制更新弹窗。**

症状很有辨识度：弹窗顶部写着「当前 0.0.0」，而设置页「关于」里显示的是 1.1.1 —— 同一个包，两个地方报的版本号不一致。

根因：`appVersion` 挂在 `Native` 对象上，更新模块却去 `API` 对象上调。调用抛异常后被 catch 兜底成 `'0.0.0'`，于是 1.1.1 的包以为自己是 0.0.0，跟线上的 1.1.1 一比就成了「大版本升级」，走强制更新路径 —— 弹层没有关闭按钮，点遮罩也没用。

改了三处：

- `API` 上补齐 `appVersion`，两条取版本路径结果一致
- 读不到版本时返回**空串**，不再编造 `0.0.0`；版本号缺失一律判为「无法比较」并静默跳过
- 更新判定增加版本号格式校验，非法的 `x.y.z` 不参与比较

### 行为调整

| 场景 | 变化 |
|---|---|
| 打开软件 | **立即**检查更新（原来要等 4 秒） |
| 已是最新版本 | 启动时**完全静默**，一个弹层都不弹 |
| 手动点「检查更新」 | 明确弹出「**您已是最新版本**」，并附上当前版本号 |
| 检查失败（断网 / 没有 Release） | 启动时静默；只有手动检查才提示原因 |

一句话：**该出声时出声，不该打扰时闭嘴。** 自动检查只在真有新版本时才打扰；手动点了则必须有回音。

### 版本号规则（不变）

| 位置 | 例子 | 行为 |
|---|---|---|
| 第一位 | `1.1.2` → `2.0.0` | 强制更新，弹层不可关闭 |
| 第二位 | `1.1.2` → `1.2.0` | 可选更新 |
| 第三位 | `1.1.2` → `1.1.3` | 可选更新 |

本次只动了第三位（修 bug），所以老版本收到的是**可选**更新提示，不装也能继续用。

### 安装

下载下方 `githup-V5.apk`，直接覆盖安装即可 —— versionCode 从 1001001 升到 1001002，登录状态保留，不需要先卸载。

### 校验

- 大小：244,169 字节
- SHA-256：`7fd36ef3955f3a06739fca123944f9d886625d24c102f3ec1b8bbdb8b1435f4f`
