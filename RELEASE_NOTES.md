## githup v1.1.1

文件名 `githup-V6.apk`，包内版本号 **1.1.1**（versionCode `1001001`），Android 7.0 起可用。

这是基线版本。**版本号从 1.1.1 重新开始**，仓库里旧的 Release 与 tag 已全部清空，只留这一个干净的发布。

### 它是什么

一个装在口袋里的 GitHub。WebView 承载前端单页应用，原生层只干 Web 干不了的事 —— 整套压缩到 **245 KB**，却能浏览仓库、看 Issue / PR、查 Actions、发 Release、上传文件，甚至让 GitHub Actions 云端帮你打包 APK。

### 更新行为（重点）

打开软件的**瞬间**就自动跟 [Buwrt/githup](https://github.com/Buwrt/githup) 对比是否最新版：

| 时机 | 有更新 | 已是最新 | 检查失败 |
|---|---|---|---|
| 打开软件（自动） | 弹更新窗（大版本强制） | **完全静默，毫无任何反应** | 静默 |
| 手动点「检查更新」 | 弹更新窗 | 明确提示「**您已是最新版本**」 | 提示原因 |

- **每次打开都真的去查一次**，不等延时、不做节流，新版本随时能在下次打开时被发现
- 从后台切回前台超过 30 分钟，也会再对比一次
- 设置页的「检查更新」按钮保留 —— 想要明确答复的时候点它

### 版本号规则

| 位置 | 例子 | 行为 |
|---|---|---|
| 第一位 | `1.1.1` → `2.0.0` | **强制更新**，弹层不可关闭 |
| 第二位 | `1.1.1` → `1.2.0` | 可选更新 |
| 第三位 | `1.1.1` → `1.1.2` | 可选更新 |

### 安装

下载下方 `githup-V6.apk`，允许「来自此来源的应用」即可安装。

> ⚠️ **如果你之前装过 V5（内部版本 1.1.2），请先卸载再装本版。** V6 的 versionCode 是 1001001，低于 V5 的 1001002 —— Android 不允许安装包版本低于已装版本。这是「从 1.1.1 重新开始」必须付的代价，之后的版本都会在此基础上递增。

### 校验

- 大小：244,917 字节
- SHA-256：`5b1768e918fe1434120b17e6a2719afae818cf80a0cef2abe3606f8640034850`
