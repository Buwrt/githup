## githup v1.1.2

文件名 `githup-V7.apk`，包内版本号 **1.1.2**（versionCode `1001002`），Android 7.0 起可用。

> 本版在 1.1.2(V7) 的基础上做了一轮**安全加固**：换成官方专属签名、开启代码混淆、
> 装包前校验签名证书 —— 别人改过的包装不上，装上也起不来。

### 防护链：一环扣一环（1.1.2 V7 最新版）

之前只有**一处**校验，别人反编译把那一处删掉就全没了。现在改成五环互相咬合：

```
第1环 签名 ──▶ 第2环 链签名 ──▶ 第3环 资源 ──▶ 第4环 身份 ──▶ 第5环 环境
  ▲                                                                  │
  └────────────────────── 闭环复检 ◀─────────────────────────────────┘
```

每一环拿到上一环的令牌，先核对令牌再干活，然后算出自己的令牌交给下一环。
**删掉中间任何一环，下一环收到的就是上上环的令牌，核对必然失败** —— 这就是"一环扣一环"。

| 环 | 校验什么 | 拦住什么 |
|---|---|---|
| 1 签名 | 当前安装包的签名证书指纹 | 改一个标点符号后重签名（**你用 MT 管理器改名字那次，就倒在这里**） |
| 2 链签名 | 链的期望值是不是官方私钥签的 | 改代码里的常量（他没有官方私钥，签不出来） |
| 3 资源 | 前端 JS/HTML 哈希清单 | 只替换前端文件塞广告、偷令牌 |
| 4 身份 | 包名 / 软件名 / 版本号 / 入口类 | 改软件名、改包名做共存版、改版本号 |
| 5 环境 | 调试器、注入框架 | 动态扒、Hook |

埋点分布在**五个地方**：进程启动（Application）、主界面、切回前台、应用内浏览器、以及每一次网络请求和令牌读写。
摘掉一个入口的校验，其他入口照样拦。

判定为篡改后，只会看到一页 **「这个安装包不是官方版本」**，
上面只有「下载官方版本」和「退出」两个按钮 —— 没有"继续使用"，返回键也退不回去。

### 这次加固改了什么

| 项目 | 之前 | 现在 |
|---|---|---|
| 签名密钥 | **debug 默认密钥**（口令 `android`，网上人人都有一份） | 官方专属 4096 位密钥，`CN=githup`，密钥不进仓库 |
| 代码 | `minifyEnabled false`，反编译出来几乎就是源码 | R8 混淆 + 资源压缩，类名全部打乱 |
| 装包校验 | 下载完直接装，包被掉包也不知道 | 下载后先比对**签名证书指纹**，不是官方签的直接拦下并删除 |
| 运行时 | 无自检 | 每次启动校验自身签名，被改过的包弹窗提示并退出 |
| Token | 明文落在 WebView `localStorage` | 只走原生 **AndroidKeyStore + AES** 加密存储，旧版残留自动清除 |
| 备份 | `allowBackup=true`，数据可被整机备份导出 | `false` + `dataExtractionRules`，禁止云端备份与换机转移 |
| 网络安全 | 默认（用户装的抓包证书也信） | `networkSecurityConfig` 只信任**系统根证书**，禁明文流量 |
| 超时 | 原生请求无超时，可能一直挂着 | 30 秒超时 |

**官方签名证书指纹（SHA-256）**

```
863dd1cd3752e59165e152bc4ab35fde478fcd296d724a6bc58cec0368184927
```

> 说明白一点：**没有任何办法做到"绝对无法反编译"**。安卓的包本来就要交给系统去执行，
> 只要有工具和时间，代码和资源总能被翻出来。能做的是三件事 ——
> 把代码搅成难读的样子（R8）、把官方身份钉死在签名证书上（改一个字节签名就废）、
> 让被改过的包在自己手机上直接拒绝运行。这三件本次都做了。

### 修了什么（沿用上一版）

**「关于 githup」点了没反应** —— 根因是弹层函数里用了设置页渲染时的局部变量 `ver`，
在那个作用域里根本不存在，一点就抛 `ReferenceError`，整个弹层都建不出来。
现在版本号统一由 `appVer()` 提供（原生桥 → `Native` → `API` 三级回落），不依赖任何局部变量。

### 关于页

「设置 → 关于 githup」现在是一个真正介绍这款软件的地方：

| 项目 | 内容 |
|---|---|
| 软件介绍 | 装在口袋里的 GitHub：浏览仓库与代码、读 Issue / PR、看 Actions、发 Release、上传文件 |
| 开源地址 | https://github.com/Buwrt/githup |
| QQ 群 | **806894257** —— 一键加群 / 复制群号，没装 QQ 也能用 |
| 赞赏支持 | 内嵌收款码原图，离线可用，长按可保存到相册 |
| 问题反馈 | 直达 Issues |
| 检查更新 | 复用已有的更新检测逻辑 |

### 更新行为（沿用 1.1.1 的规则）

打开软件的**瞬间**就自动跟本仓库对比是否最新版：

| 时机 | 有更新 | 已是最新 | 检查失败 |
|---|---|---|---|
| 打开软件（自动） | 弹更新窗（大版本强制） | **完全静默，毫无任何反应** | 静默 |
| 手动点「检查更新」 | 弹更新窗 | 底部小提示「**您已是最新版本**」，两三秒自动消失，不挡操作 | 小提示说明原因 |

- **每次打开都真的去查一次**，不等延时、不做节流
- 版本号没变但包内容换了（SHA-256 不一致）也会提示可选更新 —— 所以**不必为了发新版而改版本号**
- 从后台切回前台超过 30 分钟，也会再对比一次
- 设置页的「检查更新」按钮保留

### 版本号规则

| 位置 | 例子 | 行为 |
|---|---|---|
| 第一位 | `1.1.2` → `2.0.0` | **强制更新**，弹层不可关闭 |
| 第二位 | `1.1.2` → `1.2.0` | 可选更新 |
| 第三位 | `1.1.2` → `1.1.3` | 可选更新 |

### 安装

下载下方 `githup-V7.apk`，允许「来自此来源的应用」即可安装。本版 versionCode `1001002`
高于上一版的 `1001001`，并且**签名与上一版相同**，可以直接覆盖安装，不用卸载。

> 如果安装时提示"签名不一致"或"安装包校验失败"，说明手上的包不是官方签发的，
> 请回到 https://github.com/Buwrt/githup 重新下载。

### 校验

- 大小：683,791 字节
- SHA-256：`5208819cc715097da2b5a1f706d6d105f53176abef6faac312e1a58323d25d8c`
