## githup v1.1.1

文件名 `githup-V4.apk`，包内版本号 **1.1.1**（versionCode `1001001`），Android 7.0 起可用。

这是第一个自带更新能力的版本 —— 装上它之后，新版本不用再手动到处找包。

### 这次新增

- **内置更新检测**：App 启动后延迟几秒静默检查一次（6 小时内不重复打扰），「设置 → 检查更新」也可以随时手动检查
- **三级更新策略**，由版本号 `x.y.z` 决定：
  - 第一位变化（如 `1.1.1` → `2.0.0`）→ **强制更新**，弹层没有关闭按钮，点遮罩和返回键都没用，装完才能继续用
  - 第二位变化（如 `1.1.1` → `1.2.0`）→ 功能更新，可更可不更
  - 第三位变化（如 `1.1.1` → `1.1.2`）→ 修复更新，可更可不更
- **两个更新来源**：优先读仓库的 Release；读不到（或还没发 Release）时回退到仓库根目录的 `version.json`，APK 走 raw 地址下载
- 下载完自动拉起系统安装器，不用自己去下载目录里翻
- **完整 README.md**：功能一览、更新机制、自己编译、版本号换算规则

### 安装

1. 下载下面的 `githup-V4.apk`
2. 装不上时先给来源授权：设置 → 允许安装未知应用（Android 8.0 起的要求）
3. 直接覆盖安装就行，登录状态不会丢。旧的 V3（versionCode 10206）能被正常覆盖，因为 1.1.1 的 versionCode 是 1001001，比它大

### 校验

- 大小：243,265 字节
- SHA-256：`9831ce65f34adf098cc1638ae10f9cb4dbfadc93f76111c8d658149868e6d066`
